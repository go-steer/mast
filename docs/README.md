# mast: design docs

Design documentation for `mast` — the agent-infrastructure substrate for unattended, library-embedded, multi-provider, durable workloads. These docs describe the project as it's *currently designed*; the code lives in this repo (Phase 1 executed 2026-07-26, and the adapter ports from [`core-agent`](https://github.com/go-steer/core-agent) landed 2026-07-29 at the pinned SHAs [`./fork-design.md`](./fork-design.md) records). Ongoing sync with core-agent runs through [`./sibling-sync.md`](./sibling-sync.md).

## Reading order for someone landing cold

1. **[`./positioning.md`](./positioning.md)** — the thesis. What `mast` is, what it isn't, what gets kept / cut / reshaped from core-agent's surface. Strategy, not implementation.
2. **[`./fork-design.md`](./fork-design.md)** — the mechanics. How the fork actually happens: phasing, trigger conditions, sync discipline under (E)-sibling-products motivation, resolved decisions.
3. **[mast-web's `web-design.md`](https://github.com/go-steer/mast-web/blob/main/docs/web-design.md)** — the operator-facing UI. Why web (not terminal TUI); what we reuse from an earlier internal browser-WASM UI experiment and what we don't; stack decisions; deployment options.
4. **[`./specialists-design.md`](./specialists-design.md)** — the subagent-as-tool subsystem for mast-authored subagents. Schema, loader shape, composition with existing patterns. Coexists with skills as complementary authoring model (see #16).
5. **[`./workflow-scaffolding-design.md`](./workflow-scaffolding-design.md)** — the reference-graph library on ADK v2 primitives. Seven canonical shapes (fan-out-fan-in, sequential pipeline, supervisor+workers, autonomous loop, adversarial verifier, map-reduce, LLM-as-router) with domain wiring for mast's audience.
6. **[`./orchestration-design.md`](./orchestration-design.md)** — the unattended orchestration story: workload bundles (declarative operational profiles under `.agents/workloads/*.yaml`), the planner (supervisor-body agent with reference-graph vocabulary), bundle learning (audit-derived refinement), and evaluation + regression harness. One doc, four subsystems, one story arc.
7. **[`./durable-execution-design.md`](./durable-execution-design.md)** — the fourth pillar (unattended + library + multi-provider + durable). Pause/resume beyond HITL: programmatic pause, timed pause, external-signal pause, snapshot+replay, cross-runtime Python-ADK compat constraints.
8. **[`./observability-design.md`](./observability-design.md)** — telemetry as first-class operational surface (traces via OTel, metrics via Prometheus, structured logs, distributed tracing across MCP hops, alert integration back into the agent loop).
9. **[`./library-api-design.md`](./library-api-design.md)** — the public Go import surface for library-embedded consumers; extension points (custom providers, session stores, permission checkers, tools, specialist sources, workload sources, attach transports, observability sinks).
10. **[`./deployment-design.md`](./deployment-design.md)** — production topologies (Cloud Run, GKE, library-embedded, standalone); multi-instance coordination; multi-tenant tenancy; packaging.
11. **[`./memory-design.md`](./memory-design.md)** — mast-consumer view of the shared-memory / audit-derived-memory substrate; keyspace, reducers, state-bound reads, tenancy enforcement.
12. **[`./mcp-catalog-design.md`](./mcp-catalog-design.md)** — position doc + curated wiring templates for the MCP servers mast validates against (gke, prometheus, github, cloud-logging, slack + v0.2 expansion).
13. **[`./config-layout-design.md`](./config-layout-design.md)** — ties together `.agents/` file layout across specialists, workloads, MCP; discovery order; precedence rules; env-var overrides; hot-reload semantics.
14. **[`./a2a-design.md`](./a2a-design.md)** — Agent-to-Agent protocol integration: mast as A2A server (expose workloads as A2A skills) and client (call external A2A agents). Framework integration with Google Agent Registry / Runtime, kagent.
15. **[`./federation-design.md`](./federation-design.md)** — federation as pattern: one mast instance orchestrating N remote agents via multiple protocols (A2A, mast-native, HTTP/RPC). Planner's `invoke_remote_agent` vocabulary; topology archetypes (star / mesh / hierarchical); mast-to-mast handoff with cross-instance session state, HITL, durability.
16. **[`./skills-design.md`](./skills-design.md)** — SKILL.md format support (reinstated 2026-07-01 after GKE + Google teams began publishing skills as first-class artifacts). Coexists with specialists as complementary authoring model — specialists for mast-authored subagents; skills for consumed published templates. Publisher/consumer split, Google Agent Registry integration, policy layering (allowlist intersection, budget caps).
17. **[`./ag-ui-design.md`](./ag-ui-design.md)** — AG-UI protocol integration (agent↔user); the fourth of the four interop surfaces alongside MCP (tools), A2A (agents), and skills (task templates). Mast as AG-UI server for CopilotKit React apps + chat-platform bots (Slack et al. via `@copilotkit/channels-*`); mast as AG-UI client via federation. Interrupt lifecycle (a draft AG-UI extension — labeled bet) maps onto mast's durable pause/resume. All AG-UI ships v0.2 per the 2026-07-25 re-cut.
18. **[`./adk-v2-usage.md`](./adk-v2-usage.md)** — consolidated inventory of the ADK v2 constructs mast leans on (runner + agent modes, unified `agent.Context`, graph engine + node types, `agenttool`, HITL primitives, session events, unified span tree). Cross-references the companion docs where each surface area is decided; reference doc, not a strategy doc. Read before starting Phase 1 bucket 1.
19. **[`./model-support-design.md`](./model-support-design.md)** — combo requirements + design for model support beyond Gemini and Claude: managed providers (OpenAI, Vertex AI MaaS partner models, xAI, Bedrock, Azure) and self-hosted servers (vLLM, SGLang, Ollama, NIM). Names the eight-point bar a provider must clear to be called *supported* (R1–R8, cost and usage parity included), inventories the nine seams that assume **model-id prefix ⇒ vendor**, and proposes replacing that inference with declared **provider profiles** (dialect + endpoint + auth + tiers + capabilities). Three wire dialects rather than N adapters — `openai-chat` is the leverage, reaching Vertex MaaS *and* every self-hosted server *and* the managed long tail with one adapter. Carries the prioritized P0/P1/P2 lists and a six-slice sequencing where the mast-internal fixes (usage sidecar, profiles, backend-keyed pricing — closing [#178](https://github.com/go-steer/mast/issues/178)) land before the first new provider. **A proposal: nothing in it is settled**, and it targets v0.5+. Docs-site mirror lands with the implementation, not the proposal.
20. **[`./triage-demo-plan.md`](./triage-demo-plan.md)** — v0.1 anchor use case: mast-native reshape of core-agent's GKE triage recipe. Workload bundle + thirteen specialists (eleven per-failure-mode + `SingleTurn` classifier + `change-safety-gate` HITL) + LLM-as-router workflow graph, tying substrate and subsystems together end-to-end against a real platform-team problem. Sanctioned pre-trigger prototyping scope per [`./fork-design.md`](./fork-design.md).
21. **[`./uat-v0.2-plan.md`](./uat-v0.2-plan.md)** — v0.2 end-to-end UAT plan: deterministic, credential-free, CI-runnable acceptance suite driving the whole durable-execution spine (outbox #70, pause/abort #42, boot auto-resume #41, observability #50) through a real daemon — nine cross-cutting scenarios with state/metric/exit-code assertions, harness shape (extends `scripts/demo-spike2.sh`), and a living A2A/AG-UI growth checklist. A dev/testing plan (like `triage-demo-plan.md`); no docs-site mirror.
22. **[`./v0.3-plan.md`](./v0.3-plan.md)** — the executable v0.3 work breakdown: SRE-agent parity across mast / `k8s-lookout` / `switchboard`. PR-sized tasks (`W0`–`W8`) with file anchors, definitions of done, and the named test that proves each; a **parity scoreboard** (19 rows behind + 6 lead rows to defend) that is the release-readiness view; and a four-tier test strategy (smoke / UAT / deterministic eval / live judge). Executes [`./assessments/langchain-sre-agent.md`](./assessments/langchain-sre-agent.md) §4, with one deliberate inversion — the eval harness lands **first**, as a red scoreboard, so progress is measured against a fixed baseline. A dev/testing plan; no docs-site mirror. **Still the canonical scoreboard and the record of what shipped**, including for later releases.
23. **[`./v0.4-plan.md`](./v0.4-plan.md)** — the v0.4 work breakdown: the six remaining **mast-only** tasks, with file anchors and exit criteria. Re-cuts the milestones the v0.3 plan left for v0.4 — v0.4 = M4a + M5 (W7.0 the typed change-set producer, W7 `per_change_set`, W8 decision→eval feedback, W4.1 scheduled triggers, W4.3 the bounded path, W0.6 the J-tier cost test), v0.5 = M4b (the lookout and switchboard halves) — so the release does not depend on two other repos' schedules. Adds no test tier and no second scoreboard; rows are still flipped in `v0.3-plan.md` §1. A dev/testing plan; no docs-site mirror.

24. **[`./sibling-sync.md`](./sibling-sync.md)** — the sync-state ledger [`./fork-design.md`](./fork-design.md) § "Sync discipline under (E)" specifies: which core-agent SHAs are absorbed, which are n/a to the lean scope, which are still owed, and the security-fix correlation table. The *inventory* is generated weekly by [`../dev/upstream-drift`](../dev/upstream-drift) into a tracking issue; this doc holds the *verdicts*, because a commit count is not a decision. Start here before porting anything from core-agent. An operational ledger, not a design doc; no docs-site mirror.

Each doc has a Resolved-decisions section at the bottom listing what's been settled in conversation; the rest is open for discussion.

## Assessments

[`./assessments/`](./assessments/) holds comparative and evaluative write-ups — findings against an external system, a competitive read, a retrospective. They are *inputs* to the design corpus, not part of it: an assessment records evidence and proposes work, but nothing it proposes is settled until the owning design doc says so and the cross-reference table below records it. Like [`./uat-v0.2-plan.md`](./uat-v0.2-plan.md), they carry no docs-site mirror.

- **[`./assessments/langchain-sre-agent.md`](./assessments/langchain-sre-agent.md)** (2026-08-12) — LangChain's autonomous Kubernetes SRE agent vs. mast v0.2.0. Their subagents map ~1:1 onto specialists and mast leads on durability, cost metering, multi-provider, and read-path depth (`k8s-lookout`); the gaps are the designed-but-unwired parts — structural read/write split + per-mutating-tool gate (`hitl_policy.on_mutation`), fan-out, cross-run finding state, chat egress + Slack interactivity, typed report contract, per-specialist model tier, eval harness. Includes a six-slice MVP plan across mast / switchboard / k8s-lookout.

## Status (2026-08-19)

This repo holds mast's design corpus **and, since 2026-07-26, the mast code** (Phase 1 of the fork executed under the revised trigger). Four releases have shipped since; **v0.4.0** is current. The thesis (E) — *sibling products with divergent agendas* — remains the resolved framing: mast = platform-agent product; core-agent = experimentation/integration substrate for embedded consumers. Both maintained indefinitely.

Current state:

| Repo | Status |
|---|---|
| [`go-steer/mast`](https://github.com/go-steer/mast) (this repo) | Design corpus + shipped code. **v0.4.0** released 2026-08-17; all eleven v0.1 exit criteria green since 2026-07-29, when the P1.3 adapter ports landed. Parity scoreboard 11 of 19 ([`./v0.3-plan.md`](./v0.3-plan.md) §1); the parity claim is v0.5's. |
| [`go-steer/mast-web`](https://github.com/go-steer/mast-web) | Initialized 2026-06-12 with main scaffolding + four stacked PRs (A/B/C/C+/docs). Operator UI ships independently of the code fork; verified against a live mast daemon 2026-07-29. |
| [`go-steer/core-agent`](https://github.com/go-steer/core-agent) | Parent project; source of the completed P1.3 adapter ports. Continues as the experimentation/integration substrate under (E). Drift against the ported packages is measured weekly by [`../dev/upstream-drift`](../dev/upstream-drift); the verdicts live in [`./sibling-sync.md`](./sibling-sync.md). |

## Fork trigger (satisfied — kept for the record)

The fork has executed; this section is history, not a live gate. Per [`./fork-design.md`](./fork-design.md) (trigger revised 2026-07-26): **Phase 1's rebuild work (P1.1, P1.2, bucket-3 minimum) started immediately** — it shares no code with core-agent, and the spike-validated `mast-prototype` graduated into it. Only **P1.3 (adapter ports) gated**, on core-agent's three code cleanup milestones closing (*Correctness & durability*, *Security hardening*, *Substrate & API structure* — they churned the exact packages being ported); all closed 2026-07-28 and the staged ports landed 2026-07-29. Issues #158-#160 landed in core-agent independently and never gated; the shared-memory stack was re-homed as the gate on mast's memory work, which is still deferred.

## Resolved decisions cross-reference

A consolidated view (each doc's local resolved section is authoritative for its own scope):

| Decision | Where it's resolved |
|---|---|
| Strategic motivation = (E) sibling products | [`./fork-design.md`](./fork-design.md) |
| Project name = `mast`, repo = `github.com/go-steer/mast` | [`./fork-design.md`](./fork-design.md) |
| ADK dependency stays | [`./fork-design.md`](./fork-design.md) |
| ADK v2 from day one | [`./fork-design.md`](./fork-design.md) |
| Trigger *not* extended for core-agent's v2 migration | [`./fork-design.md`](./fork-design.md) |
| Phase-1 mechanic = rebuild-lean-core, not prune-in-place | [`./fork-design.md`](./fork-design.md) |
| Provenance via per-file attribution headers on bucket-2 ports | [`./fork-design.md`](./fork-design.md) |
| Phase 1 = P1.1-P1.6 (bootstrap, lean core, adapter ports, specialists, smoke, tag) | [`./fork-design.md`](./fork-design.md) |
| In-flight work lands in core-agent first | [`./fork-design.md`](./fork-design.md) |
| ~~Phase 1 trigger = after #158-#161 + shared-memory stack~~ *Revised 2026-07-26: P1.1/P1.2/bucket-3 start immediately; only P1.3 ports gate, on core-agent's three code cleanup milestones; #158-#160 demoted (land independently); shared-memory re-homed to mast's v0.2 memory gate.* | [`./fork-design.md`](./fork-design.md) |
| CI/release infra = independent at start | [`./fork-design.md`](./fork-design.md) |
| Interactive UI = web, not terminal | [mast-web's web-design.md](https://github.com/go-steer/mast-web/blob/main/docs/web-design.md) + [`./positioning.md`](./positioning.md) |
| ~~Skills replaced by specialists subsystem~~ *Reversed 2026-07-01: skills reinstated as first-class consumable; coexist with specialists as complementary authoring model. Rationale: GKE + Google teams publishing skills as first-class artifacts inverted the audience-fit assumption behind the cut.* | [`./skills-design.md`](./skills-design.md) + [`./specialists-design.md`](./specialists-design.md) |
| Task-class profiles shaped by v2 agent modes (Chat/Task/SingleTurn) | [`./fork-design.md`](./fork-design.md) + [`./positioning.md`](./positioning.md) |
| HITL is first-class on both plain `LlmAgent`s and workflows | [`./fork-design.md`](./fork-design.md) + [`./specialists-design.md`](./specialists-design.md) |
| Workflow scaffolding = reference graphs on v2 primitives, not helper packages | [`./workflow-scaffolding-design.md`](./workflow-scaffolding-design.md) |
| Small-tier-parent classifier → LLM-as-router with `SingleTurn` LlmAgent | [`./positioning.md`](./positioning.md) + [`./workflow-scaffolding-design.md`](./workflow-scaffolding-design.md) |
| ADK-boundary flag in sibling-sync discipline (v1/v2 API split) | [`./fork-design.md`](./fork-design.md) |
| Unattended dispatch is bundle-driven, not CLI-flag-driven; four resolution paths (explicit / envelope / bundle-selection / classifier-first) | [`./orchestration-design.md`](./orchestration-design.md) |
| Workload bundles under `.agents/workloads/*.yaml` are the operator-authored operational profile | [`./orchestration-design.md`](./orchestration-design.md) |
| Planner shape = supervisor-body with reference-graph shapes as tool vocabulary | [`./orchestration-design.md`](./orchestration-design.md) |
| Public task classes: chat, debug, implement, research, review, orchestrate; SingleTurn is internal, not user-facing | [`./orchestration-design.md`](./orchestration-design.md) |
| Bundle learning is v0.3+; requires audit-derived memory as substrate | [`./orchestration-design.md`](./orchestration-design.md) |
| Durable execution is the fourth pillar (unattended + library + multi-provider + durable) | [`./positioning.md`](./positioning.md) + [`./durable-execution-design.md`](./durable-execution-design.md) |
| Pause/resume beyond HITL: programmatic / timed / external-signal / snapshot+replay | [`./durable-execution-design.md`](./durable-execution-design.md) |
| Python ADK cross-runtime resume is future-preserved, not v0.1 scope | [`./durable-execution-design.md`](./durable-execution-design.md) |
| Observability = OTel traces + Prometheus metrics + structured logs; unified via v2 span tree | [`./observability-design.md`](./observability-design.md) |
| Library API stable-marked packages under `github.com/go-steer/mast/`; extension points at every seam | [`./library-api-design.md`](./library-api-design.md) |
| Four production topologies: Cloud Run, GKE, library-embedded, standalone | [`./deployment-design.md`](./deployment-design.md) |
| Multi-instance coordination via claim-based session ownership + timed-pause scheduler | [`./deployment-design.md`](./deployment-design.md) |
| Memory keyspace: session-scoped, tenant-scoped, global-scoped (with per-tenant opt-in) | [`./memory-design.md`](./memory-design.md) |
| MCP catalog = consume, not build; ship wiring templates + reference workloads | [`./mcp-catalog-design.md`](./mcp-catalog-design.md) |
| `.agents/` discovery order: env > project > user > system; first-match-wins, no cross-location merging | [`./config-layout-design.md`](./config-layout-design.md) |
| A2A support is first-class; mast is both A2A server (expose workloads as skills) and client | [`./a2a-design.md`](./a2a-design.md) |
| A2A integration includes Google Agent Registry / Runtime and kagent as first-order registries | [`./a2a-design.md`](./a2a-design.md) |
| A2A server build-vs-buy = hand-roll over the owned wire types (every A2A task runs through the `runTurnPre` chokepoint), not ADK's `adka2a.Executor` (which bypasses it); ships staged — Stage A = card + `tasks/get` + `tasks/cancel` + auth; Stage B1 = synchronous `message/send` (text-only ingest) + task registry + W3C trace propagation, with A2A-owned (`a2a-`prefixed) task-id fencing so a caller cannot drive turns into other surfaces' sessions | [`./a2a-design.md`](./a2a-design.md) |
| A2A Stage B2 = pluggable `a2a.RateLimiter` seam gating `message/send` only (control-plane verbs never gated), built-in per-(caller, workload) token bucket via `MAST_A2A_RATE`/`MAST_A2A_BURST`; tenant-claim → session isolation stays deferred (ADK v2.1.0 `IsolationScope` is event/task-level, not a session/tenant seam), so `Principal.Tenant` ships only as the limiter's caller identity | [`./a2a-design.md`](./a2a-design.md) |
| A2A Stage C (closes #78) = `message/stream` over SSE, sharing the `message/send` `runTask` body (differs only by an `emit` callback threaded through the turn's event loop); frames are initial `Task` → per-model-response `status-update` → closing `artifact-update` + final `status-update`; updates are message-granular (not token deltas — token streaming needs `StreamingModeSSE`, a follow-on), SSE is upgraded lazily so pre-turn refusals stay JSON-RPC errors, `message/stream` shares the `message/send` rate-limit bucket, and the card advertises `capabilities.streaming: true` | [`./a2a-design.md`](./a2a-design.md) |
| Federation is a pattern distinct from A2A; planner tool vocabulary gains `invoke_remote_agent` | [`./federation-design.md`](./federation-design.md) |
| Three federation protocol adapters: A2A, mast-native, HTTP/RPC | [`./federation-design.md`](./federation-design.md) |
| Mast-native inter-instance protocol offers richer semantics than A2A within trusted fleets (session-state propagation, cross-instance HITL, native durability) | [`./federation-design.md`](./federation-design.md) |
| Federation topologies: star, mesh, hierarchical; hybrids common | [`./federation-design.md`](./federation-design.md) |
| Skills reinstated as first-class consumable (SKILL.md format) — coexist with specialists, complementary authoring models | [`./skills-design.md`](./skills-design.md) + [`./positioning.md`](./positioning.md) + [`./fork-design.md`](./fork-design.md) |
| Specialists vs. skills = authoring model choice (mast-authored subagents vs. consumed published templates); both surface uniformly to planner | [`./skills-design.md`](./skills-design.md) + [`./specialists-design.md`](./specialists-design.md) |
| Skill discovery via A2A registries (Google Agent Registry catalogs both agents + skills) | [`./skills-design.md`](./skills-design.md) + [`./a2a-design.md`](./a2a-design.md) |
| ~~Three curated surfaces: MCP tools, A2A agents, skill templates — complementary not competing~~ *Extended 2026-07-01: four surfaces after AG-UI reinstated the agent↔user corner. MCP (tools), A2A (agents), AG-UI (user-facing surfaces), skills (task templates).* | [`./mcp-catalog-design.md`](./mcp-catalog-design.md) + [`./ag-ui-design.md`](./ag-ui-design.md) |
| AG-UI is first-class alongside attach mode; attach = mast-native rich operator UX (mast-web), AG-UI = ecosystem-standard user-facing (CopilotKit apps + chat-platform bots) | [`./ag-ui-design.md`](./ag-ui-design.md) + [`./positioning.md`](./positioning.md) |
| CopilotKit `@copilotkit/channels-*` gives mast chat-platform integration *when those packages stabilize* (revised 2026-07-25 from "for free" — the earlier `bot-*` names never shipped; Slack adapter published, others in flight); OpenTag is the reference Slack bot pattern | [`./ag-ui-design.md`](./ag-ui-design.md) |
| AG-UI interrupt lifecycle (`RunFinished{interrupt}` + `RunAgentInput.resume`) maps directly onto mast's durable pause/resume — same primitive, different wire protocol | [`./ag-ui-design.md`](./ag-ui-design.md) + [`./durable-execution-design.md`](./durable-execution-design.md) |
| AG-UI server build-vs-buy = hand-roll a zero-dep `pkg/agui` (+ shared `pkg/serverauth`) over the owned wire types, **not** the community AG-UI Go SDK — same call/reasons as A2A (every run stays on the `runTurnPre` chokepoint; the SDK would trip the slim-deps gate); supersedes the July SDK-wrap guidance + OQ #1. Ships staged under #84: Stage 1 = bundle `agui:` section + per-workload HTTP+SSE endpoint + `/agui/agents.json` discovery + shared auth/rate-limit + happy-path run stream (`RunStarted` → `StateSnapshot` → `TextMessage` triad + `ToolCall*` → terminal `RunFinished`/`RunError`) + `mast_agui_*` metrics, with `agui-`prefixed daemon-derived session ids fenced off the reserved ops-row namespace. Stage 2 = the HITL interrupt/resume lifecycle: a parked turn closes with a terminal `RunFinished{outcome: interrupt}` projected from the durable session's pending-interrupt state (superseding Stage 1's honest `RunError{interrupt}` placeholder); the client resumes with a new run carrying `RunAgentInput.Resume` entries reconciled against the session's open interrupt ids (unknown/no-open-interrupt → `ErrNotResumable`/HTTP 409, never a silent fork), recording the `interrupted` outcome on `mast_agui_runs_total` | [`./ag-ui-design.md`](./ag-ui-design.md) |
| AG-UI Stage 1 deviations: a dedicated `--agui-listen` listener (mast has no single shared HTTP root — inject/attach/a2a each own one), and a fixed `session_model` default (`per_thread`, explicit `per_run` override) since `workload.Bundle` has no task-class field to key OQ #3's task-class-aware default on | [`./ag-ui-design.md`](./ag-ui-design.md) |
| Web UI = thin client over attach mode, not WASM-as-agent | [mast-web's web-design.md](https://github.com/go-steer/mast-web/blob/main/docs/web-design.md) |
| `mast-web` phases A+B+C don't gate on the fork trigger | [mast-web's web-design.md](https://github.com/go-steer/mast-web/blob/main/docs/web-design.md) |
| Docs split: mast-design here, mast-web design in mast-web repo | This README + redirect stubs in core-agent's `docs/mast/` |
| Pre-trigger prototype lives in the standalone `mast-prototype` git repo (tags `spike1`/`spike2`), not an uncommitted scratch worktree | [`./triage-demo-plan.md`](./triage-demo-plan.md) + [`./fork-design.md`](./fork-design.md) |
| Bucket-1 ADK pin = `google.golang.org/adk/v2 v2.1.0` (spike-2 verified) | [`./fork-design.md`](./fork-design.md) + [`./adk-v2-usage.md`](./adk-v2-usage.md) |
| Graphs run as the runner root via `workflowagent.New` — no coordinator required (runner's Chat-mode rule applies to LlmAgent roots only; spike-1 conclusion reversed) | [`./workflow-scaffolding-design.md`](./workflow-scaffolding-design.md) + [`./adk-v2-usage.md`](./adk-v2-usage.md) |
| Resume model = reconstruct-and-re-execute (not deterministic replay); node bodies are ResumedInput-first with session-state stash; mutating side effects are at-least-once with declared guards | [`./durable-execution-design.md`](./durable-execution-design.md) + [`./adk-v2-usage.md`](./adk-v2-usage.md) |
| Session store = ADK `session/database` (SQLite pure-Go v0.1; Postgres = same service, pulled forward where topology demands); `pkg/eventlog/` port re-scoped to audit/query surface; shared-FS SQLite dropped | [`./durable-execution-design.md`](./durable-execution-design.md) + [`./fork-design.md`](./fork-design.md) |
| v0.1 reference-graph subset = LLM-as-router + fan-out-fan-in; HITL cannot originate inside parallel branches (`ErrParallelHITLUnsupported`) | [`./workflow-scaffolding-design.md`](./workflow-scaffolding-design.md) |
| Tool-allowlist algebra = per-field presence (absent = inherit, empty = deny, non-empty = whitelist), one normative table; per-tool MCP filtering is stock ADK `FilterToolset` | [`./specialists-design.md`](./specialists-design.md) |
| Budget substrate = event-stream `UsageMetadata` (+ `Branch`/`NodeInfo` attribution); pricing + enforcement are mast-side; post-call meter v0.1, pre-call gate follow-on | [`./orchestration-design.md`](./orchestration-design.md) + [`./adk-v2-usage.md`](./adk-v2-usage.md) |
| Triage-demo sessions are per-incident (`incident-<uid>`), not shared-session | [`./triage-demo-plan.md`](./triage-demo-plan.md) |
| Adjacent-internal-codebase references (distributed-runtime layer, embedded consumers, browser-WASM UI experiment) retired from the corpus 2026-07-27 — no longer part of mast's planning; described generically where the reasoning still needs them | [`./positioning.md`](./positioning.md) + [`./fork-design.md`](./fork-design.md) |
| P1.3 gate refined to per-package stages (2026-07-27): P1.3a (permissions/pricing/digest/taskclass/instruction/modeltier) ports immediately — the restructures that gated them are closed; P1.3b (providers/watchdog) on the open correctness bugs; P1.3c (attach) on #390/#385 | [`./fork-design.md`](./fork-design.md) |
| P1.3b landed 2026-07-29, pinned `core-agent@b8dd225` (all four cleanup milestones closed 2026-07-28). Provider construction API reshaped at port time: per-provider Options structs, no `*config.Config` constructors, no `init()` registry (mirrors core-agent #492 item 7); dispatch is an explicit switch in `internal/compose.BuildModel`; `--provider` picks the Anthropic backend, else `ANTHROPIC_API_KEY` → first-party, Vertex project → Vertex. `internal/vertexcache` → `pkg/providers/vertexcache` (public); watchdog ports with its #363 event bridge, alerts logged only (model routing = bucket 3). P1.3c: pin once core-agent's attach surface quiets | [`./fork-design.md`](./fork-design.md) |
| P1.3c landed 2026-07-29, pinned `core-agent@25d8531c` (HEAD once attach went quiet; includes #519's transport-neutral OperatorEventTarget — mast never carries the deprecated attach-named emitter). Eventlog "layer or beside" resolved: **layered** — ADK v2 `session/database` owns the tables, `pkg/eventlog` adds the seq overlay + Since/Watch (core-agent's own post-#507 shape, near-verbatim port). `pkg/attachadapter` reshaped for the runner-driven daemon (one injected message = one serialized turn; interrupt cancels the turn ctx; resume-after-restart from the session store alone, no ACL store in v0.1). Attach = single-user in v0.1; `--attach-listen` requires `--session-db`; `MAST_ATTACH_TOKEN` bearer. Exit criterion 4 verified with real mast-web (headless, proxy mode) against a live daemon | [`./fork-design.md`](./fork-design.md) |
| v0.1 scope re-cut (2026-07-25): A2A server, AG-UI (server + client), and registry publishing move to v0.2; v0.1 keeps MCP templates, `federation.Adapter` interface + `invoke_remote_agent` stub, synchronous A2A client; Phase 1 is honestly ~3-4 weeks | [`./fork-design.md`](./fork-design.md) |
| A2A integration targets the v0.3/1.0 spec (JSON-RPC `message/send` surface; no skill schemas in `AgentSkill`; bundle I/O schemas are a mast-side convention) | [`./a2a-design.md`](./a2a-design.md) |
| One planner tool for remote agents: `invoke_remote_agent(reference, inputs)`; `invoke_a2a_agent` retired | [`./federation-design.md`](./federation-design.md) + [`./a2a-design.md`](./a2a-design.md) |
| AG-UI interrupts/activity/reasoning are draft spec extensions — a labeled bet (SDK pinned, encoding isolated in `pkg/agui`); chat-platform packages are `@copilotkit/channels-*`; push + reconnect-resume are mast extensions | [`./ag-ui-design.md`](./ag-ui-design.md) |
| "Four interop surfaces" is the canonical framing (MCP / A2A / AG-UI / skills); attach mode is mast-native transport, not a surface | [`./mcp-catalog-design.md`](./mcp-catalog-design.md) + [`./ag-ui-design.md`](./ag-ui-design.md) |
| v0.1 MCP catalog entries require one pinned upstream each (gke = official Google GKE MCP endpoint); unpinnable entries demote to v0.2-candidate | [`./mcp-catalog-design.md`](./mcp-catalog-design.md) |
| Cloud Run v0.1 uses Postgres via ADK `session/database`; GKE-with-SQLite is StatefulSet+PVC | [`./deployment-design.md`](./deployment-design.md) |
| Library API v0.1 semver freeze = five pillar packages (`mast`, `agent`, `session`, `provider`, `tool`); all else experimental with named stabilization versions; `memory` experimental v0.1 → stable v0.3 | [`./library-api-design.md`](./library-api-design.md) |
| Budget/policy-critical memory keys are fail-closed; incremental reducers carry persisted event cursors (at-least-once safety) | [`./memory-design.md`](./memory-design.md) |
| Classifier-first dispatch is a privilege boundary: code-enforced per-entry-point bundle allowlists; envelope selection validated + transport-authenticated; resolutions audit-logged | [`./orchestration-design.md`](./orchestration-design.md) |
| Mutation predicate for `on_mutation`: built-in `Mutating` annotation + MCP `readOnlyHint`, default-deny-unknown, per-tool audited override | [`./orchestration-design.md`](./orchestration-design.md) |
| Mutation-approval granularity (2026-08-12) = **both**, via a new `hitl_policy.approval_granularity: per_call \| per_change_set` axis orthogonal to require/apply/dry_run. **One gate, not two**: per-change-set compiles down to per-call by minting N grants bound to exact normalized `(tool, arguments)` signatures, which the per-call gate then consumes silently. Substrate is already ported and dormant (`pkg/permissions` `Decision` scopes + `GrantStore`). **Normative restriction:** for tools the mutation predicate flags as mutating, only `AllowOnce` + change-set-minted exact-signature grants are admissible — `AllowSessionVerb`/`AllowSessionTool`/`AllowAlways` are refused (they are developer-tool ergonomics; `AllowSessionTool` on `patch_resource` hands over the namespace for a session). A change set is a **typed** `(tool, arguments)` list, deliberately a different artifact from plan-first's `plan_it_out` prose. Grants are durable/minted-not-chosen/consumed-on-use (resume-token pattern), paired against the outbox record so a crash mid-change-set knows which calls fired. `per_change_set` carries a freshness bound + precondition re-check and a legibility rule (narrow named tools only); `per_call` is the default because it has neither hazard. Phasing: `per_call` ships and earns UAT first, `per_change_set` follows in the same slice | [`./orchestration-design.md`](./orchestration-design.md) + [`./assessments/langchain-sre-agent.md`](./assessments/langchain-sre-agent.md) |
| **Approval record shape** (2026-08-13) — settled before W2 starts, because the coupling is the durable record and not the transport. Correcting the premise first: an out-of-process verdict intake **already ships** — `pkg/inject`'s `POST /resume` takes `(session_id, interrupt_id)` or a resume token plus a `Response` validated against the interrupt's `ResponseSchema` (`pkg/inject/server.go:67-97`) — so a chat transport needs no new surface and W2 is *not* coupled to the §5 M3-vs-M4 scope choice. What is coupled is what gets written down, since APIs extend cheaply and event-log rows already written do not. Two decisions: (1) the verdict is **three-valued from W2.1** — `{verdict: approve \| reject \| edit, args?, note?, approver}`, `edit` returning `not_implemented` until W2.5 — because the `ResponseSchema` is durable at pause time and widening it later is a migration across the very restart boundary W2.2 exists to survive; (2) the record carries an **authenticated approver**, resolved from `pkg/auth`'s asserted caller rather than client-supplied (`/resume` uses one shared bearer token today; `pkg/eventlog/metadata.go:34` already has the slot). Deliberately narrow: identity is in v0.3, the **session ACL store and per-session ownership are not** (§5 keeps multi-session substrate out) — they compose, because authorization arrives later as W6.3's approver allowlist and only identity is unbackfillable. **Rejected the same day: `hitl_policy` per specialist.** Policy stays workload-level; tools carry blast radius, specialists carry capability (their allowlist *is* their mutation policy), workloads carry policy. A frontmatter override is inert, or a conflict needing precedence, or a fail-open axis coarser than the audited per-tool one — and a shipped specialist declaring it would bake the bundle author's safety posture into every deployment that forks it, the `model:`-hard-binds-a-provider failure mode applied to a safety control | [`./v0.3-plan.md`](./v0.3-plan.md) + [`./orchestration-design.md`](./orchestration-design.md) |
| Cross-run finding state lives in **`k8s-lookout`, not mast** (2026-08-12) — mast is domain-neutral substrate, and fingerprinting Kubernetes findings (pod-name suffix normalization et al.) is domain logic. Reinforced by two corrections: findings outlive sessions, so mast's session store was the wrong home on its own terms; and lookout's read-only RBAC governs cluster access, not its own persistence. lookout already ships the storage layer this needs *in the single-cluster model* — the `--store` SQLite occurrence store (§9.1, TTL + size-bounded prune), `--dedup-persist`, and the `--distill-interval` recurrence→durable-fact pass (§9.2). **Durability is model-dependent** (confirmed 2026-08-12): single-cluster persists, multi-cluster holds dedup state in memory, so finding state there would not survive a restart — acks would evaporate and every finding would re-alert as new. P3's MVP therefore targets single-cluster; durable multi-cluster finding state is a named follow-on (a durable shared store, or per-cluster sentinels reporting into a central one), not an MVP assumption. Mast's side is a **wire contract, not a Go import** (`lookout findings diff --report -`), so a second domain could rehome the tracker without mast noticing. Consequence: mast never gains a `Finding`/`HealthReport` Go type — the generic `OutputSchema` mechanism stays domain-free and the concrete schema is a workload asset. A third repo is rejected on YAGNI (no second consumer today) against real CI/release/versioning cost. Ack-routing sub-decision resolved the same day — see the next row | [`./assessments/langchain-sre-agent.md`](./assessments/langchain-sre-agent.md) + [`./positioning.md`](./positioning.md) |
| **Ack routing** (2026-08-12): acks traverse mast and are forwarded to lookout — mast authenticates the caller and records the ack in its audit log, lookout is the store of record for the suppression. Two consequences are the point: the chat transport keeps exactly **one backend** (switchboard has no interactive-component event type today, so the ack path is being built either way — build it once), and every ack is **attributable** by construction. An ack is **not** a mutation approval: no `pkg/permissions` grant, no `approve \| reject \| edit` verdict, no freshness/precondition window — it is a time-boxed read-path suppression asserting no diagnosis, and collapsing the two would drag cluster-mutation controls onto a mute button. Normative: **mast must not expose an ack tool a workload's model can call** — acks enter through the operator/transport ingress only, which matters because lookout's §9.4 triage-status write is not permission-gated on its side (lookout `DESIGN.md` §614). Durability follows the finding state: single-cluster durable, multi-cluster in-memory (acks evaporate on restart) and that limitation must be named to the operator | [`./orchestration-design.md`](./orchestration-design.md) + [`./assessments/langchain-sre-agent.md`](./assessments/langchain-sre-agent.md) |
| **LangChain's `tool_coverage` does not transfer as set overlap** (verified 2026-08-12): mast's eval parity metric is **intent-level, not tool-name-level**. Three findings force it. (1) Coverage is fine — 22 of their 23 distinct `expected_tools` map to a lookout read intent, several to something strictly better (`kubectl_get_pod_logs` → `k8s_triage_logs`, 150k→350 tok; `get_cluster_summary` → `k8s_health`); the suspected NetworkPolicy and CRD gaps both close via `triage spec`'s `netpol` alias and CRD API discovery; the single non-mapping is the **write** tool `kubectl_rollback_deployment`, correctly absent from a read-only `lookout mcp` and owned by the change-executor roster instead. (2) Seven of those 23 names **do not exist in their own 63-name registry** (singular/plural slips plus an unimplemented `describe_*` family) — 16 of 71 references, ~23%, so their published number has a ceiling below 1.0 and is not an adoptable baseline; their two shipped copies of the dataset also disagree on 15 of 31 scenarios, and the `.jsonl` is authoritative. (3) Decisive: **set overlap punishes consolidation** — one `k8s_health` call answers what they answer in four, scoring 1/4 and reporting a better-factored read path as a regression, and lookout has no inventory surface by design (it reports what is abnormal, not what exists). Artifact is a checked-in intent table; name-level `tool_coverage` is emitted as a diagnostic only, never as a comparison number | [`./v0.3-plan.md`](./v0.3-plan.md) + [`./assessments/langchain-sre-agent.md`](./assessments/langchain-sre-agent.md) |
| Resume tokens: tenant-scope-bound, permission gate re-runs on resume, TTL 7d with audited override | [`./durable-execution-design.md`](./durable-execution-design.md) |
| Smaller-agents strategy: forkable standalone starters + tested slim-embed guarantee + honest routing to raw ADK — no product tier below mast, no prebuilt single-purpose binaries | [`./positioning.md`](./positioning.md) + [`./workflow-scaffolding-design.md`](./workflow-scaffolding-design.md) + [`./library-api-design.md`](./library-api-design.md) |
| v0.1 anchor use case = mast-native reshape of GKE triage (specialists + LLM-as-router + workflow graph + in-band HITL); showcase mast differentiators against a real platform-team problem, not prove parity with the core-agent recipe | [`./triage-demo-plan.md`](./triage-demo-plan.md) |
| `WithMaxConcurrency` is unreachable from a workflowagent root (`workflowagent.Config` passes no `workflow.Option`s — `workflow.New(name, edges)` bare); the per-`NewParallelWorker` `maxConcurrency` argument is the concurrency cap that actually binds (build-wave verified 2026-07-26) | [`./adk-v2-usage.md`](./adk-v2-usage.md) + [`./workflow-scaffolding-design.md`](./workflow-scaffolding-design.md) |
| **Parallel branches are invisible to the event log** (verified 2026-08-12 against ADK v2.1.0; `workflow/parallel_worker.go` is unchanged in the v2.2.0 pin, so the finding stands): `runWrappedOnce` keeps only events where `extractOutput` succeeds, and the worker emits `&session.Event{Output: output}` — no `Content`, so no `FunctionCall`/`FunctionResponse` parts. Since both recorded-effect-outbox read paths are log-derived (`scanHistory`, `ScanDangling`), the outbox **splits inside a branch**: refusal still works (`beforeTool` brackets `callTool` regardless of shape; the turn-start snapshot predates the fan-out) but recording does not — a branch-internal mutation is invisible to crash recovery, so the session looks clean when it isn't. Two normative rules: (1) a branch's `Output` payload is its only durable record, so proposed mutations (incl. `dry_run`) must be returned there, not written as events; (2) mutating-classified tools are **refused** in a parallel branch's allowlist at construction — same hazard shape and same remedy as the mutating-tool grant-scope restriction. Corollary: a `dry_run` inside a branch is *not* an interrupt origin, so it is compatible with `ErrParallelHITLUnsupported` | [`./workflow-scaffolding-design.md`](./workflow-scaffolding-design.md) + [`./assessments/langchain-sre-agent.md`](./assessments/langchain-sre-agent.md) |
| **…and that is why mast's fan-out is not built on `ParallelWorker`** (build-verified 2026-08-13, W3). The suppression above is disqualifying, not merely constraining: an LLM agent's working memory *is* the session event list (`contents_processor` rebuilds `req.Contents` from `Session().Events()`; only the runner appends), so a branch whose events are suppressed gets an identical prompt on every model call — it cannot see its own tool result, re-issues the call, and loops. No tool-using agent can be a ParallelWorker branch. `dispatch: fanout` therefore runs on `agent/workflowagents/parallelagent`, which yields sub-agent events up and waits for the append; isolation survives because it comes from branch *tagging*, not suppression. Costs: a Task specialist needs a single-node workflowagent wrapper (parallelagent calls `agent.Run`; Task completion is on the `AgentNode`/`RunNode` path) and `max_concurrency` is mast's to enforce (no cap argument). The two rules above are re-founded: payload-only synthesis is now a **contract** rather than a limitation, and mutating tools stay refused in a branch because every branch runs *before* the single post-synthesis approval gate. A third refusal joins them: `request_operator_input` is read-only and still cannot be in a branch, which has no pause the outer graph can record | [`./workflow-scaffolding-design.md`](./workflow-scaffolding-design.md) + [`./v0.3-plan.md`](./v0.3-plan.md) |
| Sessions CLI v0.1 = `mast sessions list\|show\|resume\|abort`; resume is keyed by InterruptID (`--interrupt` + `--response`), not by token — resume tokens are the v0.2 programmatic-pause surface | [`./durable-execution-design.md`](./durable-execution-design.md) |
| `pkg/session` renamed `pkg/transcript` at v0.1.0 (before the freeze — it's one of the five stable-from-v0.1 packages): ends the `mastsession`/`adksession` alias dance with ADK v2's own `session` package, mirrors core-agent's #513 rename; `mast sessions` CLI and root-package API unchanged | [`./library-api-design.md`](./library-api-design.md) |
| Abort is a durable marker + daemon-side resume refusal, not engine preemption (engine-level terminal state = v0.2 pause/abort work); list/show derive paused/aborted/idle honestly from the event log | [`./durable-execution-design.md`](./durable-execution-design.md) |
| Shutdown contract: SIGTERM drains in-flight turns up to the workload's own wallclock ceiling (30s default), pre-marks their sessions durably before waiting (SIGKILL-safe ordering), and derives an `interrupted` state (`aborted > paused > interrupted > idle`); boot-time auto-resume gates on the v0.2 recorded-effect outbox (issue #41), planned-stop classification folds into v0.2 pause/abort (issue #42) | [`./durable-execution-design.md`](./durable-execution-design.md) |
| ADK session handles are write leases (optimistic concurrency on `last_update_time`): out-of-band appends to a live session row kill the running turn with `stale session error`. All operator/daemon markers (abort + shutdown interruption) therefore write to a companion ops row (`<sid>:mast-ops`, reserved suffix, hidden from listings and refused on every session-ID surface, legacy v0.1.0 primary-row abort markers still honored) — mirrors core-agent's derived-session-ID fix for the identical subagent failure (issues #45/#46, suffix enforcement #56) | [`./durable-execution-design.md`](./durable-execution-design.md) |
| Every SQLite session-service construction — serve, one-shot, and the sessions CLI — gets write serialization + WAL + busy_timeout (shared building block: `eventlog.OpenSessionService`); SQLite lock-upgrade conflicts bypass busy_timeout and silently dropped markers/events on unhardened paths (issues #53/#64); Postgres exempt (MVCC). The daemon additionally runs ONE turn per session at a time (#62) — concurrent same-session runner turns are unsupported by ADK's OCC | [`./durable-execution-design.md`](./durable-execution-design.md) |
| The `deploy/` kustomize base is durable by default: daemon = StatefulSet + PVC at `/var/lib/mast` with `--session-db` (decided in issue #40) — the base now matches the v0.1 GKE row that already pinned SQLite-on-GKE to StatefulSet+PVC; in-memory sessions remain a deliberate opt-out (omit `--session-db`), not a deploy default | [`./deployment-design.md`](./deployment-design.md) |
| Observability v0.1 = lean seven-counter-family set (turns / model calls / tokens / cost / HITL pauses / HITL resumes / budget trips) in a fixed registry; `/metrics` shares the inject listener (separate port revisited v0.2); outcome vocabulary is `budget_exceeded`; no OTel-metrics export (Prometheus scrape + env-gated OTel *trace* export only) | [`./observability-design.md`](./observability-design.md) |
| Config discovery ambiguities resolved by the shipped `pkg/config`: set-but-missing `MAST_CONFIG_DIR` is fatal (no fallthrough); a file at a candidate `.agents` path counts as absent; workload `name:` is required (no filename-derived default for workloads); missing subsystem subdir = zero entries; env overrides are process-wide, empty = unset, negative/unparseable = fatal; mcp-server-ref validation deferred to the MCP catalog loader | [`./config-layout-design.md`](./config-layout-design.md) |
| Slim-embed guarantee shipped (reference consumer `examples/deploy/slim` + `scripts/check-slim-deps.sh` denylist + CI step); OTel denied at SDK/exporter level only, because `genai` structurally imports the OTel API; `pkg/specialists` confirmed MCP-free | [`./library-api-design.md`](./library-api-design.md) + [`./adk-v2-usage.md`](./adk-v2-usage.md) |
| Recorded-effect outbox (durable-execution open question #8, resolved 2026-08-01): the session event log **is** the outbox — durable `FunctionCall` = intent, `FunctionResponse` = completion, retention inherited, no side table; the check sits in one process-wide ADK runner-plugin tool-interception layer (the same seam the permission gate's runtime wiring takes; outbox check before gate), scoped to the mutation predicate; a dangling mutating intent puts the turn in fail-closed ambiguous-effect mode, cleared only by operator action; fresh-function-call-ID model re-issues and parallel-branch tool calls (`NewParallelWorker` never appends worker events) are honestly outside log-record coverage (handled by the turn-level mode + node-body guard + gate; mutating tools belong in sequential paths). Substrate behaviors source-verified same day: append-before-run holds on the standard path (range-over-func + runner loop ordering), ADK doesn't repair dangling calls (auto-resume repairs with synthetic responses before continuing), plugin bracket covers MCP tools. Unblocks #41. **Shipped 2026-08-01 (`pkg/effects`)** with three refinements: a `Spawning` class (sub-run tools refused in ambiguous mode — their inner calls are unguardable), MCP `readOnlyHint` unreachable in practice (ADK drops annotations at conversion; `tool_catalog.tools[].mutating` overrides are the audited un-gate), and the ack surface = `resume --ack-effects` / `mast.AckEffects` (ops-row watermark, not a transcript state) | [`./durable-execution-design.md`](./durable-execution-design.md) |
| v0.2 pause/abort mechanics (designed 2026-08-02; issue #42's planned-stop classification folded in): ADK v2.1.0 affords no mid-turn park from arbitrary code and no terminal session state (source-verified), so the surface is **two pause planes** — in-turn interrupt pause (`pause_session` long-running builtin + the RequestInput node seam) and out-of-turn gate pause enforced at the turn chokepoint (`runTurnPre` under the session turn lock; `Interrupt: true` also cancels the in-flight turn); resume tokens are minted-not-chosen (`mrt_` + 128-bit random, ops-row records, scope-checked (app, user) before execution, 7d TTL that `PauseSpec` may only shorten, consumed on resume; `extend-token` is the audited override; OQs #1/#2/#5 resolved, #9 ratified); timed pause = single-instance daemon min-heap firing through the normal budget-wrapped resume paths; terminal abort = chokepoint refusal of ALL turn kinds (v0.1 gap: only resume refused) + in-flight cancel, marker-first ordering; planned stop = `mast stop` → the SIGTERM drain path with `"operator stop"` markers, exit `3` on drain-expiry-with-survivors (exit codes encode work-cut-short, not initiator), `--pause-sessions` gates the marked set out of #41 auto-resume; the `paused` derivation gains two sources — unanswered `LongRunningToolIDs` (fixing the shipped v0.1 gap where long-running parks projected `idle`/`interrupted`) and the gate marker — and #41's dangling-call repair must exclude long-running calls or it destroys deliberate pauses (adversarial gate findings, applied pre-merge). **Shipped 2026-08-02** (pkg/transcript pause records/tokens + derivation extension; chokepoint + cancel registry + `/pause` `/extend-token` `/stop` + token resume; timed-pause scheduler; `pause_session`; `mast.Pause`/`ResumeByToken`; `mast stop` + exit 3) with three recorded deviations: per-request token scan instead of an in-memory index (single-instance scale; P1.3 owns fleet indexing), `extend-token` daemon-only, `already_resumed` = HTTP no-op but a library error | [`./durable-execution-design.md`](./durable-execution-design.md) |
| Boot-time auto-resume (issue #41, shipped 2026-08-04 — the v0.2 durable-execution closer, gated on the recorded-effect outbox): on boot the daemon scans for `interrupted` sessions and drives a continuation turn for each eligible one through the same `runTurnPre` chokepoint (turn lock + budget meter + outbox plugin) every other turn kind uses; **on by default** (`--auto-resume`, `--auto-resume=false` disables), serve + durable `--session-db` only (no `mast.go` library twin — consumers own their process lifecycle). The guarantee is the operational form of exactly-once: **auto-resume never double-fires a mutation** — a session with **any** dangling mutating call is skipped (`skipped_ambiguous`) regardless of ack watermark (an ack suppresses the outbox refusal but does not pair the call), left for operator `ack-effects`; a dangling read-only call on the single last function-call event is repaired with a synthetic `interrupted before completion` response and re-run; a completed-turn-with-stale-marker is cleared (no spurious "Continue" turn); a genuine trailing user/paired-tool turn re-invokes with nil msg. Rails: freshness window (`--auto-resume-window`, default `1h`; skips stale-at-crash work), per-session restart-loop breaker (3/10m, durably pre-incremented) + per-boot turn cap, and a `preTurn` supersession recheck under the turn lock (`skipped_superseded` on concurrent advance). Slice-1 = **coordinator dispatch only** (`skipped_unsupported` for graph/foreign-UID/deferred-delegation; graph auto-resume, idempotency-keyed relaxation, and a durable global boot-frequency breaker are documented follow-ons). `mast stop --pause-sessions` opts sessions out (gate pause outranks `interrupted`). Metrics: `mast_autoresume_total{workload,outcome}` (8-outcome fixed vocabulary; #50 canonicalizes all v0.2 families). New seams: transcript `ScanInterrupted`/`Summary.InterruptedAt`/`RecordAutoResumeAttempt`/`ClearAutoResumeAttempts`; effects `ScanDangling` (shares `scanHistory`'s pairing core, which keeps its exact shipped output) | [`./durable-execution-design.md`](./durable-execution-design.md) |
| Observability fixed-registry v0.2 pass (issue #50, shipped 2026-08-04): the v0.2 durable-execution surface (markers, pause planes, timed scheduler, auto-resume) is canonicalized into **five fixed counter families** — `mast_autoresume_total{workload,outcome}` (8 outcomes; shipped earlier with #41), plus the **four added by #50**: `mast_marker_write_failures_total{workload,operation}` (operation ∈ `mark`/`clear`/`pause`; closes the previously-silent lost-marker gap — this family is the inverse, advancing only when a write *failed*), `mast_aborts_total{workload}`, `mast_gate_pauses_total{workload,source}` (source ∈ `operator`/`planned_stop`), `mast_timed_pause_fires_total{workload,outcome}` (outcome ∈ `resumed`/`skipped`/`error`) — each incremented at the write site (a counter advances only when the durable op happened), all primed to zero per workload, registry still fixed (no caller-minted names/labels). Shipped names supersede the pre-impl `mast_pauses_total`/`mast_resumes_total` sketch (split by emitting mechanism, not a `reason` label; pause-duration histograms stay deferred). Also adds a **teardown watchdog**: after the bounded drain, `serve()` arms a 15s watchdog over the deferred teardown (OTel flush, eventlog/attach `Close`, context cancels) that dumps goroutine stacks + force-exits with a dedicated code `4` (distinct from drain-expiry `3`) on overrun — a wedged `Close`/leaked goroutine surfaces a diagnostic instead of a silent hang until supervisor SIGKILL; rejected alternative = rely on SIGKILL. Deferred follow-ons: plane-A `pause_session` metric (lives with the planner surface), a broad `mast_resumes_total` (would double-count `hitl_resumes`) | [`./observability-design.md`](./observability-design.md) + [`./durable-execution-design.md`](./durable-execution-design.md) |
| v0.2 end-to-end UAT (plan drafted 2026-08-05): a deterministic, credential-free, CI-runnable acceptance suite (`scripts/uat-v0.2.sh` + a `dev/ci/presubmits/e2e.sh` job) drives the whole v0.2 durable-execution spine through a real daemon — nine cross-cutting scenarios (crash-mid-mutation→ambiguous→ack, crash-mid-read→auto-resume, planned-stop precedence, drain exit codes 0/3/4 + 503, timed pause across restart, gate-pause 409 + token lifecycle, terminal abort, restart-loop breaker, metric priming/cardinality) asserting session state + `/metrics` counters + process exit codes. Offline by contract: `--model=echo` for no-tool flows, scripted provider (`MAST_SCRIPT` JSONL) for the mutating-call flows the echo model can't emit; SQLite `--session-db`; `--dispatch=coordinator` (v0.2 auto-resume is coordinator-only). Race-shaped legs (exit-3, teardown-watchdog exit-4) degrade to Go-test assertions if no clean env-gated deadline hook exists. A2A/AG-UI scenarios grow into the same harness as those land. Plan doc only for now (a dev/testing plan, no docs-site mirror); harness is the follow-on | [`./uat-v0.2-plan.md`](./uat-v0.2-plan.md) |
| Local (stdio) MCP + generic catalog wiring (issue #87, shipped 2026-08-09): mast wires every MCP server a workload references from the `mcp.json` catalog generically, dispatched by transport — superseding the prior single hard-coded HTTP `gke` toolset. Two transports: streamable **HTTP** (optional Google OAuth/ADC bearer, the GKE path) and local **stdio** (mast launches `command`+`args`+`env`, `${VAR}`-expanded against the daemon env, and speaks MCP over the child's stdin/stdout). MCP stays unwired under `echo` (no tool calls); `scripted` + real providers wire it, so a stdio server drives tools fully offline. A workload referencing a server absent from `mcp.json` is a fatal load error; `mcp.json` is a privilege-bearing control-plane file (stdio = code execution) and each launch is logged. Unblocks the deferred blocking-tool UAT legs. New `pkg/mcp` catalog loader + `NewToolset` dispatch (`NewGKEToolset` now a thin HTTP wrapper) | [`./mcp-catalog-design.md`](./mcp-catalog-design.md) "Implementation status" |
| Per-specialist `model:` override honored + cross-provider allowed (specialists-design open Q#4, resolved 2026-08-12 with v0.3 W1.1): `pkg/specialists.Build` had been parsing `model:` and building every specialist with the parent's model, so "cheap analysts, frontier synthesis" was unreachable. The override now resolves through a `specialists.ModelResolver` injected by `internal/compose` — the same `BuildModel` dispatch `--model` uses, which dispatches on the model id, so **cross-provider overrides are legal** (a `gemini-*` specialist under a `claude-*` parent). Refusing them would have meant a provider-family classifier as a second source of truth beside that dispatch, and would fence off the multi-provider property mast leads on. Three constraints: resolution is **memoized per model id** (eight analysts on one tier share one client); a declared override that cannot be resolved **fails the build** rather than silently inheriting the parent (silent inheritance is the bug being fixed, and credentials for every provider in the roster must therefore resolve at construction, not mid-incident); and under an **offline-fake parent** (`echo`/`scripted`/`toolactor`) every override collapses back to the parent so a tiered bundle still runs credential-free for the offline S/U/E test tiers. Left open: `model:` names a concrete id and so hard-binds a bundle to one provider — a provider-portable `tier:` field over `pkg/taskclass.ModelForTier` is the proposed fix, not built | [`./specialists-design.md`](./specialists-design.md) open Q#4 + [`./v0.3-plan.md`](./v0.3-plan.md) W1.1 |
| **Specialist `capability:` is a declared, enforced field** (new in specialists-design, shipped 2026-08-14 with v0.3 W2.4): `read_only` (**the default**) or `change_executor`. `internal/compose.CheckCapabilitySplit` runs inside `BuildRoot` — every construction path, every dispatch shape — and refuses a roster where a read-only specialist can reach a mutating tool: by naming one, by taking a whole MCP server (its future tools are unreviewed), or by declaring no `tools.mcp` while the bundle ships a `tool_catalog` (inheriting the catalog inherits its write tools). Escape hatches: enumerate, write `mcp: []`, or declare `change_executor`; `SingleTurn` is exempt (no tools). "Mutating" is the same default-deny-unknown predicate the outbox and write gate use, so an unclassified catalog fails the roster rather than the incident. **Declared rather than inferred from the allowlist**, because the declaration is what makes the write surface auditable — every `change_executor` is logged at startup, so "who here can change the cluster" is a log line, not an intersection of three files. This turns the 2026-08-13 row above ("specialists carry capability — their allowlist *is* their mutation policy") from a convention into a check; the `pkg/graph` fan-out branch rule stays stricter (it refuses even a declared `change_executor`, since branches run before the one gate). Two fail-open enforcement bugs found and fixed the same day: `mcp: []` had been implemented as inherit-all (the deny-all row of the normative table granted the catalog), and the non-empty MCP whitelist had **never matched a real toolset** — ADK's `mcptoolset` names every instance `"mcp_tool_set"` and mast never renamed them, so an enumerating specialist got no MCP tools at all | [`./specialists-design.md`](./specialists-design.md) "Capability" + [`./v0.3-plan.md`](./v0.3-plan.md) W2.4 |
| **The `edit` verdict is executable, and refuses rather than narrows** (shipped 2026-08-14 with v0.3 W2.5): an operator answering `{"verdict":"edit","args":{…}}` at `/resume` gets *their* arguments run in place of the model's. Four gates stand in front, each a refusal: no authenticated approver → `edit_unattributed` (mast will not run operator-authored arguments it cannot attribute — enforced for `edit` only, since `approve`/`reject` execute a call the model proposed and a policy pattern already vetted); an argument the tool does not declare → refused, checked by mast rather than left to the schema, because an MCP server's schema usually omits `additionalProperties: false`; a value the declared input schema rejects → `edit_refused`; and the **edited** call is re-adjudicated against the deny policy under its own call key, so `deny scale_deployment(deployment=prod-*)` cannot be defeated by editing an approved staging call into a production one. Because ADK resumes by re-firing the original `FunctionCall` verbatim, the durable event records the model's arguments while the operator's execute — so the gate writes an `AppliedEdit` (proposed call, executed call, both argument sets, approver, note) into the state delta of the event ADK is already appending, and `mast sessions show` prints it. **The read side is part of the fix, not a follow-on:** a durable record nobody surfaces leaves the operator reading the model's arguments off the transcript exactly as before | [`./orchestration-design.md`](./orchestration-design.md) "Write gate, as shipped" + [`./v0.3-plan.md`](./v0.3-plan.md) W2.5 |
| **The cluster-side mirror of the read/write split, and what actually bounds it on GKE** (shipped 2026-08-14 with v0.3 W2.6): diagnosis is a cluster-wide read `ClusterRole` with **no secrets**; change is a namespaced `Role` in its own kustomization (`deploy/remediation-target/`), instantiated once per namespace mast may change, deliberately narrower than the tools it backs — `apply_manifest` can name any kind, the Role admits workload objects and ConfigMaps in one namespace and no Deployment deletes. It is **not** in `deploy/base` because the base pins `namespace: mast-triage` on everything it renders, so a Role carried there would land in the daemon's own namespace and an operator retargeting it would widen the base for every deployment forked from it. `deploy/rbac_test.go` walks from the *subject* — every ClusterRoleBinding naming the daemon's SA — since the erosion mode is a cluster-scoped grant added for one tool, not an edit to the file called "read". **The finding that reframes the row: Kubernetes RBAC is not currently what bounds this agent.** GKE allows a call if *either* IAM or RBAC allows it, and the daemon reaches the cluster through the GKE MCP server as its WIF principal, which `setup-wif.sh` binds `roles/container.admin`. The narrowing (`WRITE_SCOPE=namespaced` → `roles/container.viewer`, writes carried by RBAC) ships as an opt-in and **not** as the default, because whether GKE resolves that principal to the RBAC subject the RoleBinding names is untested against a live cluster and this project does not ship unexercised enforcement as a guarantee. Disclosed in the SA manifest, printed by the script on every default run, and failed as a cell by `scripts/rbac-matrix.sh` when `PROJECT_ID` is set | [`./deployment-design.md`](./deployment-design.md) "Cluster permissions" + [`./v0.3-plan.md`](./v0.3-plan.md) W2.6 |
| **A remediation must be proposed as data, not as prose** (settled 2026-08-14, from the first live run of the anchor workload against a real GKE cluster): `change-executor` was unreachable in both shipped dispatch shapes — graph specialist nodes are terminal with no edge to it, and the coordinator's default instruction, written before W2.4 created it, says "return analysis only". The gate machinery is real and UAT-proven; nothing generated a call for it to catch. **The fix is not a coordinator prompt.** W2.4 removed the diagnosers' write tools precisely because prompt text is not a boundary, and wiring the forward path with prompt text is that mistake mirrored — it also cannot work, because `finding.json`'s `recommended_actions` is `array<string>` and the live run produced `"Suggest 'kubectl rollout undo …'"`: prose, for a human, naming a verb the MCP server does not expose. Instead the finding gains a typed `proposed_change` — `(tool, arguments)` drawn from the workload's own catalog and schema-validated on return — so the graph edge becomes a structural predicate, the operator approves the object that actually fires, and W7's grant minting has a producer. Scoped as **W7.0**, which now blocks W7. **Shipped 2026-08-15**: `pkg/approval/changeset.go` owns the contract, the check runs as a `finish_task` before-tool callback so a bad proposal is refused *back to the specialist*, the accepted set is recorded durably per specialist, and `pkg/graph` routes on it — proven end to end by `scripts/uat-v0.4.sh` (`U-handoff/A`: the parked question quotes the approved `replicas=2`, not the `replicas=10` the same model picks for itself, and approving it fires `apply_change` once) | [`./v0.3-plan.md`](./v0.3-plan.md) W2 finding (c) + [`./v0.4-plan.md`](./v0.4-plan.md) W7.0 |
| Docs site = **Astro ^7 + Starlight** (core-agent's actual `docs/site` convention; the corpus's "Hugo"/"Hugo + Docsy" references were stale — corrected 2026-07-26); v0.1 skeleton shipped at `docs/site/` (landing + install + 3 quickstarts + reference + roadmap); CI = build + internal-link verification (`ci-docs.yml` via `dev/tools/docs-site.sh check` + `scripts/verify-internal-links.py`, added 2026-07-28 with the ported `remark-prepend-base` plugin after the `/mast` project-pages base broke root-relative content links); deploy live at go-steer.github.io/mast since 2026-07-27, gated on the same verifier; house rule #4 (walk design docs + site together) is now active | [`./fork-design.md`](./fork-design.md) Phase 0/Phase 4 + `AGENTS.md` house rule #4 + `docs/site/README.md` |
| **Positioning is stated positively; mast is not positioned against named products** (2026-08-14). The "not a Claude Code competitor" line — top-of-fold on the site, in the README's routing list and its "what mast is not" section, and in the thesis — is gone from every artifact. Rationale: the clause spent the reader's first thirty seconds on a comparison rather than on the job, dated fast, and made a scope decision read like a defensive one. **The scope boundary it encoded is unchanged and still written down**: interactive-editing UX is out (no LSP, no AST tooling, no syntax-aware diff UI; `edit_file`/`write_file` exist to serve unattended workloads), and the honest routing to raw ADK v2 for a simple loop stays — that one names an upstream dependency, not a rival. Applies going forward: state what mast is for; describe an out-of-scope shape without naming or ranking whoever occupies it | [`./positioning.md`](./positioning.md) + [`./fork-design.md`](./fork-design.md) Phase 4 + `README.md` + `docs/site/` |
| **v0.4 is mast-only; parity moves to v0.5** (re-cut 2026-08-14, after v0.3.0 shipped). The v0.3 plan left v0.4 = M4 + M5, but four of M4's six tasks (W4.2, W4.4, W4.5, W4.6) are the mast-side halves of surfaces k8s-lookout and switchboard have not landed, so taking it literally makes v0.4's ship date a function of two other repos' schedules — the same problem the M1–M3 cut solved for v0.3. **M4 splits rather than being renumbered:** M4a (W4.1 scheduled triggers, W4.3 the bounded path, W0.6 the J-tier cost test) joins M5 (W7.0, W7, W8) in **v0.4**; M4b (the other four W4 tasks + W5 + W6) is **v0.5**, and keeps the parity claim. Scoreboard rows 1, 8, 10, 19 are v0.4's; 9 and 11–17 are v0.5's; one lead row (L7, approval bound to a typed change set) is added when W7.0 lands. Two conventions carried in rather than re-litigated: each release gets its own acceptance script (`scripts/uat-v0.4.sh`, run after v0.2's and v0.3's by `e2e.sh`), and the J-tier cost test is renamed **`J-cost-tier`** to match §2's tier-prefix rule | [`./v0.4-plan.md`](./v0.4-plan.md) + [`./v0.3-plan.md`](./v0.3-plan.md) §5 |
| **Three things the change set settled while being built** (2026-08-15, v0.4 W7.0). (1) **`arguments` travels as a JSON *string*, not an object** — a free-form object has no schema that admits it: ADK's report validation refuses keys the output schema did not declare, and mast's own roster loader refuses an object property with no declared properties because it would accept anything. Mast parses the string and validates it against the real tool's real input schema, which is the stronger check; the object spelling is still accepted on the way in, because that is what mast writes into durable records. (2) **Everything about the field fails closed** — no name→schema resolver means every proposal is refused, an absent or empty list leaves the finding untouched, and a roster with two change executors logs an error and leaves the diagnoser→executor edge unwired instead of guessing (`Build` still succeeds: the two-executor shape is legal, and refusing to start would break rosters already in the acceptance pass). (3) **The event handing approved calls to the executor must not be user-authored** — ADK's confirmation resume bails on a user-authored event with no FunctionResponse, so a user-role announcement silently disables the resume: nothing fires, no error. `genai.RoleModel` plus `ConvertForeignEvent` is the fix, pinned by `TestApprovalAnnouncementIsNotUserAuthored` | [`./v0.4-plan.md`](./v0.4-plan.md) W7.0 findings (a)–(d) + [`./spike-findings.md`](./spike-findings.md) |
| **"Fresh" is two clocks, and the cluster one is declared by the bundle** (shipped 2026-08-15, v0.4 W7): approving one parked call with `scope: change_set` mints a grant for **each remaining call**, bound to an exact normalized `(tool, arguments)` signature — durable, single-use, expiring, and re-checked against the cluster before it fires. The wall clock (`hitl.change_set_ttl`, default 10m) is the **backstop**; the actual check is a **declared precondition** — `precondition: {read, args, args_from, fields}` on the tool in the workload's own `tool_catalog`, snapshotted at mint and re-read at fire. Declared rather than inferred because mast cannot synthesize it: it does not know which tool reads the object a write is about, nor which of that write's arguments names it, and the bundle knows both. A tool that declares none gets the TTL and nothing else, and the parked question **says so** rather than implying a check mast is not making. Three structural consequences. The grant sits **in front of** the W2 gate: deny policy adjudicates first, and spending a grant still records `permissions.DecisionAllowOnce` — a grant removes the *question*, never the accounting. A `change_set` verdict is **refused whole rather than narrowed** when any call in the set is un-grantable, since a partially-honored "approve all of this" is the one outcome the operator did not choose. And a precondition over the field the set itself rewrites is **self-invalidating** — the snapshot is taken once, so call 1 voids call 2 by succeeding — which means each call must be checked against *its own* object; that is what `args_from` is for, and why the live fixture scales two Deployments rather than one. Two declaration mistakes fail closed at startup instead of at the incident: a precondition read not declared `mutating: false` (an unlisted tool *is* mutating, so a catalog that merely forgot the read is refused), and a `fields:` path wrong about ADK's `{"output": …}` wrapper around structured MCP content — a precondition over a tool returning none silently compares nothing to nothing | [`./v0.4-plan.md`](./v0.4-plan.md) W7 + findings (a)–(e) + [`./orchestration-design.md`](./orchestration-design.md) "Write gate, as shipped" |
| **A fifth test tier: C — cluster** (added 2026-08-15 with v0.4 W7; [`./v0.3-plan.md`](./v0.3-plan.md) §2's S/U/E/J list is otherwise unchanged). Two of W7's claims cannot be made offline: a precondition declared over a real read tool has to be right about a JSON shape mast does not control, and "the cluster moved" means *somebody else ran `kubectl scale`*, which a harness writing its own fixture file cannot honestly stage. `scripts/live-kind-v0.4.sh` stands up a throwaway kind cluster and runs both. **It is not tier U and must not become it**: U is credential-free, dependency-free, runs on every PR and gates the build; C needs a container runtime, takes minutes, and **writes to a Kubernetes cluster** — so it is opt-in (`MAST_LIVE_KIND=1`), deliberately absent from `dev/ci/presubmits/e2e.sh`, and not a gate. The isolation discipline is taken verbatim from the sibling `core-sre-agent` project (`github.com/go-steer/core-sre-agent`, not yet published), which arrived at it the same way: **a fault injector must never touch a real cluster**, and the pin has to be mechanical rather than advisory. So the script creates its own cluster, refuses to adopt or delete one whose name is not `mast-live-*`, writes a single-context kubeconfig under `/tmp` it refuses to overwrite, and the MCP fixture (`testdata/live/kubemcp`) independently re-checks the context prefix and that exactly one context exists before it will start — passing `--kubeconfig` and `--context` on every invocation from an environment built from scratch. **The ambient `current-context` is never resolved, on any path** | [`./v0.4-plan.md`](./v0.4-plan.md) §2 + [`./v0.3-plan.md`](./v0.3-plan.md) §2 |
| **A specialist declares how much model it is worth, not which vendor's** (shipped 2026-08-15, v0.4 W1.1a — the `tier:` question v0.3 W1.1 finding (b) left open, promoted when W4.3's "one cheap-tier call" became its consumer). `tier: small \| mid \| frontier` ships **beside** `model:`, not instead of it: an exact id is the right answer for a bundle with a reason to pin one, and the wrong answer for a shipped bundle, where the id is a vendor choice imposed on everyone who forks it. The tier resolves at build time through `pkg/taskclass.ModelForTier` — the same table `--task` uses, so the two cannot disagree — against a provider family derived from the explicit `--provider` alias, else the root model id's prefix, which is the dispatch `BuildModel` already makes. Declaring both spellings on one specialist is a **load error, not a precedence rule**: they are two answers to one question and no reading of the file says which lost. Resolution runs *through* the memoized model resolver rather than beside it, so W1.1's properties carry over unchanged (one client per resolved id; unresolvable fails the build; offline fakes collapse it — checked *before* the family is derived, since `echo` has no family). **The load-bearing half is the meter:** `MeterScopes` priced off `s.Model`, so a tiered specialist would have run cheap and been billed at the parent's rate — right bundle, right run, wrong number — and both paths now go through one `SpecModelName`. Shipped exercised, not merely declared: `examples/workloads/ns-audit` is four `tier: small` analysts and a `tier: mid` synthesis, and the resolved id is logged per specialist at startup because it depends on how mast was started | [`./specialists-design.md`](./specialists-design.md) "Model and tier" + [`./v0.4-plan.md`](./v0.4-plan.md) W1.1a |
| **A check whose verdict is arithmetic may gate the judged nightly; a check whose verdict is judgment may not** (settled 2026-08-16, v0.4 W0.6(a)). [`./v0.3-plan.md`](./v0.3-plan.md) §2 says the J tier reports and never gates, and that rule is intact — it exists so a model having a bad day cannot turn `main` red. `J-cost-tier` gates anyway, and the distinction is not the tier but what the verdict rests on: it runs a `tier: small` analyst under a `tier: frontier` synthesis, reads the meter's per-scope snapshot back, and divides. A specialist whose tier resolved to the cheap model and whose tokens were billed at the parent's rate is a **mast bug with a live reproduction**, and nothing about saying so depends on what the model answered. So its findings become harness Problems and fail the nightly job while every corpus score stays report-only. Three companion rules keep the check from passing vacuously, each of which is a way it could have shipped green and meaningless: **resolved is not ran** (a tier that resolved to haiku and executed as opus prices correctly and costs wrongly, so each row carries the provider's reported `ModelVersion`, prefix-matched); **a roster whose tiers all collapse onto the root compares nothing** and is reported as proving nothing rather than as passing; and **zero calls is a finding, not a pass**. And because the offline fakes collapse every tier onto one model — the same condition that makes the free tiers credential-free is the one that erases what this measures — an offline root is a **stated skip**, printed as such, never a silent absence | [`./v0.4-plan.md`](./v0.4-plan.md) W0.6 + [`./v0.3-plan.md`](./v0.3-plan.md) §2 |
| **`bounded` is a fourth dispatch shape, not a mode on the one-shot path** (shipped 2026-08-16, v0.4 W4.3 — scoreboard row 10). A workload that has to cost one cheap model call is asking for a *shape*: exactly one `SingleTurn` specialist declaring an `output_schema:` and a `tier:`, built as a one-node workflow with no orchestrator above it. Spelling that as a flag on `internal/compose/oneshot.go` would have made every property a runtime condition inside a path that also serves unbounded rosters — the roster would be checked while the agent tree was already half-built, and the failure would read as a runtime surprise instead of a refusal to start. As a shape it is four checks in `internal/compose/bounded.go` that run *before* construction, each naming what it found: a planner enabled (the orchestrator the shape is defined by not having), a roster that is not exactly one specialist (it prints the count and the names), a spec in Task mode (an absent `mode:` renders as "Task (the default for a spec with no `mode:`)", because the bundle author wrote nothing and needs to be told what nothing means), and a missing `output_schema:` (without it "one call" buys an unparseable paragraph). **`RosterShape` never infers it** — a one-specialist roster is a perfectly ordinary coordinator, and inferring `bounded` would silently impose a cost ceiling nobody declared; the shape is opt-in in the bundle's `dispatch:`, and the non-inference is pinned by a test so a later "helpful" heuristic has to delete an assertion to land. **The count is asserted off the meter, never estimated:** `Result.Usage.ModelCalls == 1` in Go, `"session_model_calls":1` in the daemon log, and `mast_model_calls_total{workload="bounded-triage"} 1` on `/metrics` — three observation points, one number. Second finding: **mast wired MCP before refusing a roster it could never build**, so in a credential-free environment the reason "mast will not start" came back as an OAuth 403 against the hosted GKE MCP server; `compose.CheckRoster` is now a static pre-flight the binary runs before wiring, and `BuildRoot` re-runs it so the library path is not weaker. First finding: **the offline fakes could not answer a forced response schema at all** — with `ResponseSchema` set, `echo` and `toolactor` returned their usual prose and ADK failed the turn on output validation, so the shape was untestable offline until both fakes learned to emit schema-conforming JSON through the existing `pkg/agent/schemafill.go` filler (`MAST_FAKE_SCHEMA_VIOLATION` still forces the negative). Exercised by `examples/workloads/bounded-triage` (one `tier: small` specialist, the shipped `finding.json` contract unchanged, no `scheduled:` block) and the `U-bounded-cost/steps` acceptance leg | [`./v0.4-plan.md`](./v0.4-plan.md) W4.3 + [`./specialists-design.md`](./specialists-design.md) "Model and tier" |
| **An exported decision names the change, not the person** (shipped 2026-08-16, v0.4 W8 — the privacy question that stanza said to settle before shipping). Every adjudication the write gate makes is a durable `approval.Decision`, and `mast sessions export-decisions` writes them as JSONL with a `_meta` provenance header. **Approver identity is redacted by default**: a stable truncated sha256 (`sha256:` + 16 hex), the digest shape `pkg/approval/grant.go` already uses. Stable, so a consumer can still group by approver, count distinct approvers or spot the identity that approves everything — without a name; and the default is the safe one because a default that has to be remembered is not a default. `--include-approver` opts into raw identities and `_meta.redaction` records which mode produced the file, so a redacted export can never be mistaken for a raw one. **`mast:`-prefixed machine identities pass through in the clear** (`mast:internal`, `mast:scheduler`): they name a mechanism rather than a person, and digesting them would hide the one distinction a reviewer of an unattended fleet most needs — whether a human was in the loop at all. **Argument values are preserved**, deliberately: the proposed→executed pair is the entire label, and an export with arguments stripped records only that somebody edited something. So an export is exactly as sensitive as the arguments the fleet's tools take — cluster topology included — and that is said in the file (`_meta.warning`), enforced at `0600` on `--out`, and printed on the user-facing page rather than mitigated by a second knob. Two structural decisions come with it: the record is **written beside `AppliedEdit`, not instead of it** (`AppliedEdit` exists only for substitutions, so a harvest over it would export edits and no refusals — and it is a shipped surface `mast sessions show` prints), and a **change-set grant being spent writes its own record** under `authority: change_set_grant`, so a file cannot show one approval where four calls ran. v0.4 ships **capture and export only** — no scoring, no retraining, no feedback into prompts; the file is inert and mast never reads it back | [`./v0.4-plan.md`](./v0.4-plan.md) W8 + [`./orchestration-design.md`](./orchestration-design.md) "Write gate, as shipped" |
| **A missed tick is skipped, not caught up — and the timed-pause scheduler still catches up** (shipped 2026-08-16, v0.4 W4.1, the decision the task existed to make). `edge_trigger.scheduled: {interval, jitter, prompt}` wakes a workload with nothing calling in. When the daemon is down across three ticks it fires **none** of them, logs one line naming the count and the window, and resumes on the anchored cadence: a periodic run samples the *current* state of the world, so a sample nobody took is owed to nobody, while catch-up on boot means a crash-looping daemon buys a fresh backlog of model runs at every restart — about the crash. `cmd/mast/pausesched.go` keeps the opposite behaviour deliberately, because a timed pause is a promise about one specific parked session nobody else will keep. Two more halves are load-bearing: the **anchor is persisted at seed** (fires land on anchor + k×interval, so a restart resumes the original phase and does not re-phase a nightly sweep into the afternoon — writing it at first fire instead would let a daemon redeployed faster than its interval never fire at all), and the fire, never the anchor, carries the jitter. A fire opens a fresh session as **`mast:scheduler`** — a namespaced identity no human can take — through the same `runTurnPre` chokepoint every other turn kind uses, so a mutating call in a scheduled run still parks for a real approver. Single-instance, like the pause scheduler | [`./v0.4-plan.md`](./v0.4-plan.md) W4.1 + [`./v0.3-plan.md`](./v0.3-plan.md) §1 row 8 |
| **Upstream drift is measured, not diffed — and the `agent-substrate` deferral now has an instrument** (2026-08-17). `dev/upstream-drift` reads the `// Originally derived from go-steer/core-agent@<SHA>[:<upstream-path>]` trailers and asks `git log <sha>..origin/main -- <path>` per file; a diff would be 100% noise because every ported file differs by construction, and an upstream rename silently reads as "in sync" unless reported as its own `missing-path` status. Weekly, into one edited-in-place tracking issue, never failing a build — drift is the expected state here, and much of it is code P1.3 will re-port wholesale. This does **not** reopen the shared-library deferral (house rule #7): the 6-12 month clock is three weeks in and the stability condition is unobserved. It makes it observable, and records the two constraints the deferral doesn't state — a shared module touching ADK/genai/mcp-sdk forces mast (v2) and core-agent (v1) into version lockstep, so the plausible first slice is the stdlib-only code; and extraction needs core-agent to agree to consume it, or it is a repo split rather than a shared library | [`./fork-design.md`](./fork-design.md) "Sync discipline under (E)" |
| **The first triage: 48 upstream commits, and what measuring them cost and bought** (2026-08-17, [`./sibling-sync.md`](./sibling-sync.md)). Every commit has a verdict — 20 absorbed or ahead, 13 n/a to the lean scope, 7 to port, 7 watchdog commits held pending a governance call, 1 security roundup split across two ports. Four findings generalize. (1) **The detector's headline catch was a live P0 in mast's own code**: every tool mast defines reached Claude as `{"type":"object","properties":{}}`, because ADK v2's `functiontool.New` populates `ParametersJsonSchema` and leaves the typed `Parameters` field nil. It survived two green judged nightlies and a live GKE run because ADK's *internal* declarations — `finish_task`, the one the eval harness asserts on — do use the typed field. Nothing errors; Anthropic accepts an empty input schema and validates nothing against it. (2) **A trailer is a port-batch baseline, not per-file provenance**, so a re-port that doesn't bump it makes the detector report absorbed drift forever — [#149](https://github.com/go-steer/mast/pull/149)'s ten files were the largest false-positive class, and "a re-port bumps the trailer" is now a rule. (3) **Port timing is not a security control**: mast got five sixths of core-agent's #465 roundup for free because `pkg/attach` was ported two days after it, and missed the sixth because `pkg/permissions` was ported two days before — the plan-first `fetch_url` exemption, closed here. (4) **`fork-design.md`'s sync table now has a member on the wrong side of the fork**: it lists watchdog→model routing as a *lean-fork-specific* feature, and upstream shipped it as `e42a511`. Port-and-reclassify, build-mast's-own, or drop the example — a strategic call, so all seven watchdog commits are held rather than ported piecemeal | [`./sibling-sync.md`](./sibling-sync.md) + [`./fork-design.md`](./fork-design.md) "Sync discipline under (E)" |
| **Port all seven watchdog commits, and reclassify watchdog→model routing as shared infrastructure** (2026-08-17, resolving the governance question the triage raised). The classification said mast-only; upstream independently shipped it. **A feature both repos want is shared by definition** — the table was a prediction, and this is the prediction being checked. Leaving a wrong row in place costs a feature that lands twice, in two shapes, and diverges silently. `fork-design.md`'s lean-fork row now names classifier-first dispatch on task class instead, which follows from mast's own dispatch model rather than from a guess about core-agent's roadmap. The ports are adaptations, not cherry-picks, and the adaptation is the same shape every time: **upstream hangs this state off its `Agent` struct, and mast has no `Agent`** — turns are free functions over a runner event stream. So enforce-mode halt state becomes a `watchdog.Enforcer` the daemon holds per session, the pending-observation queue becomes a `watchdog.Feedback`, the refusal moves from an agent preflight to the `runTurnPre` chokepoint every turn kind funnels through, and the halt is the caller's `cancel()` — the package decides *whether* a session is halted and why, never how a turn dies. Three signals where upstream has one also means three model-facing `Guidance` strings, and mast's operator-facing `Reason` names `POST /sessions/{id}/interrupt`, which is exactly the kind of affordance the model-facing half must not mention. The postures are a ladder — `warn` < `feedback` < `enforce`, each including the last — and **`enforce` implies `feedback` deliberately**: a halt is cleared by an operator reset, and a reset resumes a model whose context still ends in the loop it was halted for, so a reset without the injected observation is a treadmill (which is also why a reset clears the halt but keeps the queue) | [`./sibling-sync.md`](./sibling-sync.md) + [`./fork-design.md`](./fork-design.md) "Sync discipline under (E)" |
| **A metric whose corpus states no basis for its labels is a diagnostic, not a comparison number** (2026-08-19, closing [#179](https://github.com/go-steer/mast/issues/179)). `severity_accuracy` has read near half on every judged board since v0.3, with the misses running one direction — expected WARNING, got CRITICAL — on two unrelated model families. Two readings fit that equally: the ported corpus's labels contradict the severity definitions it ships with, or mast's rubric escalates past them. The ask was to partition the misses between them, against the definitions rather than the labels. **The partition is not derivable, and finding that out is the result.** The definitions are twelve enumerated conditions taken verbatim from upstream; measured over the 31 scenarios, the CRITICAL conditions occur in 5 and the WARNING and INFO conditions in none, while 20 rows are labelled WARNING or INFO. On almost every row that misses there is no stated definition to check either claim against, so the two readings are indistinguishable *in principle* rather than by this metric — and where the rubric does decide a row it contradicts the label twice in five. So the metric keeps its column and its per-row comment and stops being a parity claim, the disposition `tool_coverage` has. Tuning the rubric until it moves stays rejected (v0.3, teaching to the test) and would now also be tuning against nothing. **Two general rules came out of it.** *Reach is necessary and not sufficient*: the metric scored all 31 rows throughout, and was still both unparseable on one common verdict format and unactionable in aggregate — `MetricReach` cannot see either. *A silent extraction failure is indistinguishable from a wrong answer*: `## SEVERITY: CRITICAL` read as "declared no severity" and scored 0, exactly as a wrong verdict does, on 7 of 31 rows of one board and 0 of 31 of another — so a harness defect published as a cross-family capability gap. The demotion is pinned by `judge.TestSeverityRubricDoesNotSpanCorpus`, which fails if the corpus or the rubric moves toward covering the other; that is the condition for promoting it back | [`./v0.4-plan.md`](./v0.4-plan.md) §6 + [`./assessments/langchain-sre-agent.md`](./assessments/langchain-sre-agent.md) "Dataset integrity" |
| **Durable state that outlives a process must not outlive the config that admitted it** (2026-08-19, closing [#180](https://github.com/go-steer/mast/issues/180)). mast's hub peer registry is now optionally durable — `attach.NewPeerRegistryWithState` snapshots registrations to a JSONL file and reloads them at startup, so a restart no longer un-federates the deployment for a heartbeat interval. Ported from core-agent `9f81626`, with one deliberate divergence, and the divergence is the decision: **a reloaded lease is re-clamped to the running configuration's max TTL rather than replayed at the width it was granted.** A lease is a grant — "this peer counts as live until *T*" — and [#166](https://github.com/go-steer/mast/issues/166) already settled the general form for budget grants: replaying a grant against a configuration that no longer supports it is arithmetic on a number that stopped meaning anything. Upstream reloads the recorded expiry as written, so lowering the ceiling and restarting honours the withdrawn grant twice — once for the outstanding lease, then indefinitely, because `Heartbeat` re-derives the TTL from what it reloaded. The clamp runs before the expired-lease drop and is written back on the eager first snapshot, so it survives its own process. **The narrowing direction gets the opposite treatment and needs no argument**: the owner is replayed verbatim, because carrying a restriction forward can only restrict — and dropping it would have silently undone the enumerate-then-delete hardening, which is why the on-disk record is a separate type from the wire `Peer`. Only the library half of the port landed: `cmd/mast` has no peer-hub flag, so upstream's `--attach-peer-state-file` has nothing to hang on and adding one is a feature, not a port | [`./sibling-sync.md`](./sibling-sync.md) "`9f81626` — a durable grant is still a grant" |
| **Reach is satisfiability, not declaration — and a dead diagnostic reports without gating** (2026-08-20, closing [#174](https://github.com/go-steer/mast/issues/174)). `MetricReach` is the guard against a column that is green because it never ran; it reported mast's own `tool_coverage` as `31/31 scenarios scorable` while that column returned 0.000 on every row of every board since the corpus was ported. The cause is a definition rather than a line. Reach counted a scenario as reached when the scenario had *declared* an expectation, and every row declares one; the question is whether the expectation can be *met*, and no row's can — the dataset names upstream's 23 `kubectl_*` tools and mast's read surface is `k8s_*`, an empty intersection on all 31. `intent_coverage` sat beside it in the same file without the bug, because it builds its denominator *after* mapping names through the intent table, so **the asymmetry was the defect rather than either metric**. `ToolCoverage` now takes the intent table and marks a scenario vacuous when no expected name is one the runtime can emit, which `lookout_tools` answers by construction (`judge.Rig` builds its fixture tools from those keys and `judge.NewCluster` refuses to start on one it has no shape for). **The score deliberately does not move**: an unsatisfiable expectation still reports 0/N and still names the tools, because scoring a consolidated read path as a regression is the reason this diagnostic is on the board at all — what changes is that the harness stops averaging the 0 into a mean and says out loud that the column is a constant. Second half, and the one with teeth: a dead *diagnostic* is now reported on `CorpusSummary.DeadDiagnostics` instead of appended to `Summary.Problems`. [#179](https://github.com/go-steer/mast/issues/179) added that report describing it as non-gating and put it on the list `Summary.OK` reads; it was latent only because no diagnostic was dead yet, and `tool_coverage` is dead permanently — so the fix above would otherwise have turned the whole deterministic tier red on a fact about two tool surfaces. **The general rule, which is [#179](https://github.com/go-steer/mast/issues/179)'s stated one arriving from the other side**: reach is necessary and not sufficient, and a guard that measures a property adjacent to the one that matters cannot see itself | [`./v0.3-plan.md`](./v0.3-plan.md) W0.2/W0.4 + `internal/evals/measurability.go` |
| **A promotional rate is a fact about the future, and the regen has to say which numbers moved** (2026-08-20, closing [#188](https://github.com/go-steer/mast/issues/188)). `Rates.UpdatedAt` answers "when did we last read this from LiteLLM" and cannot answer "is this still true"; LiteLLM publishes no expiry metadata on any row (verified against the live 1.7 MB catalog), so a scheduled price change exists in mast or nowhere. **What #188 proposed was a `ProvisionalUntil` field on `Rates` with a hand-curated overlay; what shipped is the other half of its proposal plus a second exit on the existing dated assertion**, because the enumeration the issue asked for changed the shape of the problem. Three rows turned out to be promotional and none behaved as expected: `claude-sonnet-5`'s increase was **cancelled** (the introductory $2/$10 is now standard, so the assertion would have gone red on 2026-09-01 over a correct rate — `resolved` records that and inverts the pin), while `gemini-3.7-flash` and `gemini-3.6-flash` are **both** on $0.75/$3.75 until 2027-01-01 and then double, the first of them being the gemini/vertex frontier default. **The load-bearing lesson is how the Gemini pair was missed**: #184's regen halved `gemini-3.6-flash` and the auto-PR body said only "Automated regeneration", so a rate joining an introductory tier was read as a permanent cut and written into `pkg/taskclass`'s frontier justification, wrong within a day. `--report` now names the rows whose values changed at the top of the auto-PR, which is the signal that would have prompted the question. A field on `Rates` is still the right home for operator-visible provisionality and is deferred, not refused — the argument for it is now stronger than when #188 was filed, since the exposure is a doubling on mast's own default rather than a hypothetical | `pkg/pricing/introductory_rates_test.go` + `dev/regen-builtin-pricing/main.go` + [`.github/workflows/pricing-regen.yml`](../.github/workflows/pricing-regen.yml) |
| **Who a durable approval names comes from the credential, not the body — and not from the token either** (2026-08-20, closing [#194](https://github.com/go-steer/mast/issues/194) and the OQ #9 residual [`./durable-execution-design.md`](./durable-execution-design.md) left open). `PauseRecord.ConsumedBy` is the audit answer to "who approved this?" — the whole reason a durable gate beats an in-memory prompt — and the daemon wrote the constant `"operator resume --token"` into it on every HTTP resume, naming the channel on a field whose only job is naming the human. #194 listed a `resumed_by` field on `ResumeRequest` first; **that option is refused, and the refusal is the decision**. An attribution a caller writes about itself is worth nothing after an incident, which `pkg/attach`'s `GuardrailResetRequest.Caller` (`json:"-"`) had already settled in the other direction on the other surface. The original OQ framing — mint an operator-identity claim *into the token* — fails for a second, sharper reason: a token is a bearer capability, so anything minted at pause time is a claim about whoever *later* holds it, which is exactly the person it cannot know; a token handed to a colleague must attribute the colleague. **So the identity is read where it is actually known, at the moment of the request**, and the plumbing already existed on both ends: `pkg/inject`'s `handleResume` resolves the caller onto the request context (including the `X-Asserted-Caller` proxy path a chat relay needs) and `cmd/mast`'s `approverFromContext` renders it — the daemon simply never read it, which is why the fix is one call and the tests are the deliverable. The shared `auth.Attribution(ctx, fallback)` renders all three shapes and takes a **mechanism name** as its fallback rather than `"unknown"`: a caller-less context is not a failed lookup but an in-process path (timer, boot auto-resume, an embedder), and the code always knows which one it is. Two literals deliberately stay: `"timer"` is pinned normatively by this doc's timed-pause response shape, and the direct-DB CLI's own name is honest about a path with no credential to read. The gap this left — nothing wired an `auth.Authenticator` into the daemon's inject listener — is closed by [#198](https://github.com/go-steer/mast/issues/198) below | [`./durable-execution-design.md`](./durable-execution-design.md) "Resume tokens" + `pkg/auth/attribution_test.go` + `cmd/mast/resume_attribution_test.go` |
| **A tool-declaration invariant is asserted over a captured catalog, and the same invariant for every adapter** (2026-08-20, closing [#168](https://github.com/go-steer/mast/issues/168), the gating layer of [#172](https://github.com/go-steer/mast/issues/172)). [#154](https://github.com/go-steer/mast/issues/154) shipped a total tool-calling outage to a release: every `functiontool.New` and every MCP tool reached Claude as a bare name with an empty input schema, because `toolsParam` handled the typed `genai.Schema` spelling and ADK v2 populates `ParametersJsonSchema`. The regression test that came with the fix pins `functiontool.New` — **the shape of that bug rather than the shape of the bug class**, which is the standing lesson the fix left unapplied. Two decisions make the replacement hold. First, **the catalog is captured, not enumerated**: `internal/toolcatalog` drives a coordinator+MCP rig and a planner rig through a real ADK runner with a recording model and reads what the model was handed, so no list of construction paths exists to be forgotten — the paths ADK installs itself (`finish_task`, the delegation tool) are reachable *only* this way, since they live in an unimportable `internal/llminternal`, and a tool wired into either shape joins the catalog on the next run. Expectations are derived from each declaration rather than written beside it, so they cannot go stale; the rigs' own tests are the anti-vacuity floor, since a catalog that quietly stopped producing tools would turn every provider test green while measuring nothing. Second, **`toolcatalog.Verify` is shared rather than reimplemented per adapter**: an adapter held to a weaker check than its siblings is how a defect hides, and #154 is the proof — the Gemini path was correct throughout, so nothing measured against it would have caught the Anthropic converter. Each adapter supplies only a reader for its own wire spelling. Gemini's copy is a dependency probe rather than a test of mast code (nothing there converts anything) and earns its place because `ParametersJsonSchema` is a pass-through `any` whose loss nothing would report. Offline, credential-free, ordinary presubmit; the Anthropic test fails on eight of ten tools with the #154 fix reverted | [`./model-support-design.md`](./model-support-design.md) R2/R8 + `internal/toolcatalog/` + `pkg/providers/{anthropic,gemini}/toolwire_test.go` |
| **A tool call is measured by its arguments and its result, and the violations are listed rather than averaged** (2026-08-20, closing [#169](https://github.com/go-steer/mast/issues/169), the second layer of [#172](https://github.com/go-steer/mast/issues/172)). The judged board recorded which tools a run reached for and nothing about how, which left two different failures looking identical: the model never called the tool, and the model called the right tool against a scope that matched nothing, read the empty result as "no problem here", and moved on. Only the first is a tool-*selection* problem. Three decisions. First, **the result is retained alongside the arguments** — `evals.Call.Response`, which also carries ADK's `error` key, so a failed call stays distinguishable from an empty one; arguments alone say what was asked for and only the result says whether anything was learned. Second, **this is not a metric**: `evals.ValidateCalls` enumerates violations (unknown tool, missing required, wrong type, value outside a declared enum, invented argument, error, empty read, no completion) and the board prints them, because averaging discards the only actionable part — which tool, which argument, which scope — and a rate over rows that call different numbers of tools is not comparable to itself run over run. Malformed calls are listed individually and empty reads are counted, since an empty read is ordinary triage right up until *every* read in a row is empty, at which point the row's score measures what the model already knew. Third, **the empty-read signal rides beside the fixture's prose rather than inside it** (`Cluster.ReadResult`): a `found` flag in the tool's payload, or different wording for a clean reading, would change what the model reads and move every score on the board in the same run that introduced the column. The schemas validated against are read off the built tools, not written beside them. Redaction follows W8's already-settled answer rather than a new one — arguments verbatim, since a violation whose scope is redacted is one nobody can act on — with the board stating its own exposure, which for a synthetic corpus is none | [`../internal/evals/validity.go`](../internal/evals/validity.go) + `internal/evals/{trace,harness/judge,judge/rig}.go` |
| **A skipped tool is not a miss; an unanswered question a tool would have answered is** (2026-08-20, closing [#170](https://github.com/go-steer/mast/issues/170), the third layer of [#172](https://github.com/go-steer/mast/issues/172)). The obvious metric — did the run call the tools that had data for this scenario — was computed against both v0.4.0 boards before being rejected, and **the data is the argument**: it flags 11 of 31 scenarios on Claude and 18 of 31 on Gemini, and the flagged rows score *higher* on `response_quality` (0.886 vs 0.838; 0.986 vs 0.962). That is the consolidation thesis showing up in the measurement — 14 of the table's 19 intents are satisfiable by two or three different lookout tools, so most skipped tools were redundant with one that had already answered. Counting them is noise with a plausible name, the same trap `tool_coverage` fell into one level up. What the board reports instead is a **consequential miss**: an expected intent left unanswered *when a tool in the catalog would have answered it*. Filtered to that, the whole population across both boards is four rows, all node or pod saturation — so it is **printed as a list, not averaged**, with the scenario, the intent, and the tools that would have served it, on the same reasoning [#169](https://github.com/go-steer/mast/issues/169) settled for violations. The partition is the substance: an unsatisfied intent no read-only tool serves is a statement about the catalog rather than the model (LC-13 expects a rollback; lookout excludes write tools by design) and is counted separately, beside the structural ceiling that already reports it — confusing "the catalog cannot answer this" with "the model did not ask" would publish a scope decision as a permanent regression. A third bucket, an expected name `intents.yaml` has never seen, is a gap in the table and is neither. `IntentCoverage` and `ClassifyMisses` now read one shared `coverage`, because a score and a list derived independently from the same corpus will eventually disagree and the disagreement would be invisible | [`../internal/evals/misses.go`](../internal/evals/misses.go) + `internal/evals/harness/{judge,delta}.go` |
| **A miss is attributable to a tool only when exactly one tool would have answered it** (2026-08-20, closing [#171](https://github.com/go-steer/mast/issues/171), the fourth layer of [#172](https://github.com/go-steer/mast/issues/172)). Once [#170](https://github.com/go-steer/mast/issues/170) reduced the population to four rows, the obvious next question is which tool each one is — and the answer is that **three of the four are the same tool**, `k8s_resource_top`, the only thing in the catalog that answers either saturation intent, skipped independently by two unrelated model families. **The attribution rule is the decision, and it is a restraint**: with 14 of 19 intents served two or three ways, charging a miss to each candidate server would triple the ranking's numbers and name no cause, so a multi-server miss is **counted as shared rather than blamed** — visible, unattributed. The ranking then leads with **sole-source × gated-scenario-count** rather than with the miss tally, because how many scenarios a tool is the only answer for is a property of the corpus while how often it was missed is one model on one night, and the first is what says whether the fix is worth an afternoon. Only tools with at least one sole-source intent appear at all: a tool whose every intent has an alternative can be skipped for free by construction, and listing it at zero would pad the ranking with rows that can never move. Gating is computed from `intents.yaml` and the dataset alone — never from a board — and counts a scenario once however many of its intents need the tool. **What this buys is an experiment the scores cannot show**: `judge.toolDescription` lives in this repo rather than in k8s-lookout, so the description can be rewritten and the board re-run, with the nightly delta printing `k8s_resource_top: 3 miss(es) → 0`; a fix worth keeping moves `intent_coverage` by a fraction of one row's mean and would otherwise read as noise. The competing hypothesis — that the models reason *past* the tool rather than fail to find it — is separated by the same experiment | [`../internal/evals/misses.go`](../internal/evals/misses.go) `GatingBy` + `internal/evals/harness/{judge,delta}.go` |
| **A user table says who approved; it is not a second way in** (2026-08-20, closing [#198](https://github.com/go-steer/mast/issues/198) and the gap [#194](https://github.com/go-steer/mast/issues/194) left). `MAST_INJECT_USERS_FILE` + `MAST_INJECT_PROXY_IDENTITIES` wire an `auth.Authenticator` into the daemon's own inject listener, so a CLI-launched daemon can record `alice@example.com` instead of `shared-bearer-token`. **Why nothing had wired one before is a defect, not an omission**: the shared-token gate and `BearerTokenAuth` both read `Authorization: Bearer`, so configuring both made `/resume` unreachable by *either* credential — the user token failed the gate, and the shared token cleared the gate and then failed authentication. The fix splits the gate for that one route (`resumeAuthOK`) and **deliberately does not widen the others**: `/inject`, `/abort` and the rest still take `MAST_INJECT_TOKEN` alone, because attribution is not authorization. **The shared token keeps working and keeps saying so** — a resume presenting it is still `shared-bearer-token`, since configuring a table must not retroactively attribute an unmigrated emitter's approvals to a person; the strict posture is reached by *not setting* `MAST_INJECT_TOKEN`, and is then total rather than partial. Three restraints fall out. A shared-token request carrying `X-Asserted-Caller` is **403, never silently ignored**: a credential that cannot name its own holder does not get to vouch for someone else's, and ignoring the header would record the shared token while the relay believed it had named a human. Admin identities are **not read** even though `NewBearerTokenAuth` takes them — nothing on the inject path consults `Caller.Admin`, and an unenforced authorization knob reads as enforcement that is not there. And both env vars are validated **at startup** (proxies with no table, or a proxy identity absent from it, refuse to boot) because the runtime symptom is a 403 with no clue attached. Env rather than a config key only because mast has no daemon config file; core-agent's `BuildMultiSessionAuthn` is the same shape behind a `table_file` key | [`../pkg/inject/server.go`](../pkg/inject/server.go) `resumeAuthOK`/`callerContext` + [`../cmd/mast/injectauth.go`](../cmd/mast/injectauth.go) |
| **A catalog is enumerated by the code that does the wiring, or it drifts** (2026-08-20, closing [#137](https://github.com/go-steer/mast/issues/137)). `GET /sessions/{id}/tools` listed MCP tools and nothing else, so a planner-dispatch daemon with no MCP servers answered 200 with an empty list — true of its MCP tools, and wrong as an answer to "what can this daemon do". mast's non-MCP tools are the planner's whole control plane. **The issue's second option, a hand-maintained list beside the endpoint, is refused**, on the v0.3 `tool_catalog` finding: a list written next to the consumer drifts from the list written next to the constructor, and a catalog naming tools that do not exist is worse than one omitting tools that do. So `planner.Vocabulary(cfg)` is exported and **`planner.New` calls it** — the enumeration and the wiring are not two lists that agree, they are one function — and `compose.BuildRoot` returns what it wired alongside the root, since ADK offers no way to pluck the tools back off a built agent. Builtins are listed **unconditionally**: they carry neither the 30s TTL nor the partial-failure handling the MCP half needs, because unlike a server they cannot fail to answer or vanish between polls, and they stay in the response when every server is down — the case that motivated the fix. They sort ahead of every server for free, carrying no `Server`. The construction runs through `rootBuild.catalog` rather than inline in `serve` **because [#133](https://github.com/go-steer/mast/issues/133)'s bug was a field `serve` never assigned**, and a catalog missing its builtins still returns 200 with a plausible list; a method is a thing a test can call. Still out of reach and deliberately not faked: the tools ADK installs itself (`finish_task`, a coordinator's delegation tools), which need [#51](https://github.com/go-steer/mast/issues/51) / [adk-go#1229](https://github.com/google/adk-go/issues/1229) | [`../pkg/planner/planner.go`](../pkg/planner/planner.go) `Vocabulary` + [`../cmd/mast/toolcatalog.go`](../cmd/mast/toolcatalog.go) |
| **`retryable` is a decision, and a deliberate stop is not one to re-take** (2026-08-20, closing [#206](https://github.com/go-steer/mast/issues/206)). `ClassifyTurnError` mapped `context.Canceled` to `transient_network` / `retryable: true`. mast has two live producers of a cancelled turn and **neither is a network fault**: an operator interrupt over attach, and `--watchdog=enforce` cancelling in flight on a Critical alert. The flag exists to drive exactly one client decision — offer a re-run or don't — so a client keying off it was being invited to re-drive the loop the watchdog had just halted. A new `canceled` kind carries `retryable: false`; the `DeadlineExceeded` branch beside it is **deliberately untouched and pinned by a test**, because a turn that ran out of time is one nobody asked to stop. **The asymmetry is the decision**, not the new enum value. The protocol goes 1.4.0 → 1.5.0, additive in the same way `cost_ceiling` was: no new event type, no new field, and §2.6 already requires an unrecognized kind to read as `unknown`, so an older client has a defined fallback rather than undefined behavior. **What the bump also forces into the open is that the two version lines have diverged**: core-agent shipped this same value at *its* 1.8.0, having spent 1.5.0–1.7.0 on session titles, ACLs and wake frames mast does not have, so a number no longer names the same feature set on both sides and `DESIGN.md`'s wire-compatibility claim is now scoped to *shape* — clients feature-detect against the capabilities frame. Found by the 2026-08-20 drift triage as upstream `04e54a3`, and ported for mast's own reasons: upstream's motivating producer is a TUI's ESC key, mast's is an unattended watchdog | [`../pkg/attach/errors.go`](../pkg/attach/errors.go) + `pkg/attachadapter/adapter_test.go` `TestInterruptedTurnIsNotReportedAsRetryable` |
| **An error mast raised itself is not a string to be guessed at** (2026-08-20, closing [#208](https://github.com/go-steer/mast/issues/208)). `pkg/watchdog` raises a distinct `TrippedError` type precisely so a host can tell "an operator must reset this" from "retry this" — and `ClassifyTurnError`, the one place that decides what an operator reads, ignored the type and substring-scanned the message. A halt's message is built from **the looping tool's name and up to 200 bytes of its model-supplied arguments**, so three of four realistic halts came out as `config_error`, `model_not_found` or `auth_error`, each with a hint sending the operator to debug Vertex config or an IAM binding during a runaway-loop incident; the fourth came out `unknown`, there being no `watchdog_halt` kind at all. Worse, the remedy the reason does carry sits past `firstSentence`'s 240-byte cap and never arrived. Fixed with a `SelfClassifyingError` interface consulted via `errors.As` **ahead of every heuristic** — string matching is the right default for errors that arrive from outside, and the wrong one for errors mast wrote. **The interface carries the kind and nothing else**, deliberately: naming a `TurnError` would force a raiser to import `pkg/attach`, and `pkg/watchdog` is stdlib-only — the same coupling `errors.go` already refused in the other direction for `pkg/budget` ([#135](https://github.com/go-steer/mast/issues/135)). The weaker bare-string contract is paid for by a pin in `pkg/watchdog`'s own tests. `pkg/budget`'s `ErrExceeded` stays on its prefix match: it is a wrapped sentinel, not a named type, with nothing to hang a method on. Protocol 1.5.0 → 1.6.0, additive on the same terms as [#206](https://github.com/go-steer/mast/issues/206). Upstream fixed its own version of this at `6d30f9b` | [`../pkg/attach/errors.go`](../pkg/attach/errors.go) + `pkg/watchdog/classify_test.go` `TestAHaltIsNotReportedAsAProviderProblem` |
| **A model id is stamped to be priced, so it has to be a key that resolves** (2026-08-20, closing [#210](https://github.com/go-steer/mast/issues/210)). `ModelVersion` is taken from the model the API echoes back rather than the one requested, because where they disagree the server's answer is the billed one — an alias resolves server-side to a dated snapshot. But that field is read as a **pricing key**, and `pricing.Catalog` matches by exact key then by longest prefix *anchored at the start of the string*: `claude-opus-4-5@20251101` finds its row, and a Vertex resource path (`projects/{p}/locations/{l}/publishers/anthropic/models/…`) finds nothing, since no key is a prefix of a path. mast ships `anthropic-vertex`, and the catalog's own regenerator already drops `/` ids as router noise from the other direction. An echo containing `/` now falls back to the requested id. Bounded, not an incident — `Meter.priceOf` degrades to the flat per-1k rate and counts the turn `unpriced` rather than billing $0, so a ceiling still trips — but the degradation discards every per-model rate including cache reads, the largest term on a cache-warm agent. The test asserts the property (`pricing.Catalog` can resolve whatever was stamped) rather than the string, since a stamp nothing can look up is the same outcome as no stamp. Found verdicting core-agent `6e609f3` as "mast is already ahead"; the general per-profile identity rule (Azure deployment names, vLLM paths, Vertex MaaS ids) is still owed | [`../pkg/providers/anthropic/llm.go`](../pkg/providers/anthropic/llm.go) + [`./model-support-design.md`](./model-support-design.md) identity table |
| **An allowlist axis with no subsystem behind it is refused, not accepted** (2026-08-20, closing [#211](https://github.com/go-steer/mast/issues/211)). `attach.ToolSourceSkill` was declared and produced nowhere, and reading why turned up the larger fact: **mast has no skills subsystem at all** — no `pkg/skills`, no SKILL.md loader, no `invoke_skill`, no CLI — while [`./skills-design.md`](./skills-design.md) schedules the loader for **v0.1** and four releases have shipped without it. Three surfaces were declared as though it existed. The one that matters is `ToolAllowlist.Skills`: on the single file whose job is to say what a sub-agent may touch, two axes are enforced (`builtin` by the write gate and capability split, `mcp` by `filterToolsets`) and the third is read by no production code, so `skills: [a, b]` looked exactly like a whitelist and narrowed nothing. The loader now **refuses a non-empty list**, by the same discipline it already applies to a misspelled `capability` or `tier` — a declaration that cannot take should fail at startup naming the file. `skills: []` stays loadable, because present-but-empty means deny-all on every axis and deny-all is what this build does. The other two — the wire enum, which an embedder or core-agent's daemon can legitimately still produce, and `pkg/permissions`' `"skill"` plan-exempt namespace, whose whole-namespace exemption should be re-decided when the loader lands — are annotated in place rather than deleted. No user promise broken: the docs site never advertised skills, and no shipped spec or fixture uses the axis | [`./specialists-design.md`](./specialists-design.md#the-skills-axis-does-not-exist-yet-2026-08-20) + [`../pkg/specialists/loader.go`](../pkg/specialists/loader.go) |
| **The turn is the span mast has to open itself, because the unattended turn has nothing above it** (2026-08-20). ADK's spans parent off whatever context reaches `runner.Run`, and mast handed it two different kinds. Request-driven turns — inject, resume, attach, A2A, AG-UI — arrive under otelhttp's server span and read as a tree. The turns the product exists for do not: a scheduled fire, a boot-time auto-resume and `mast run` start from a daemon or process context with no span, so `invoke_agent` became a **trace root** — every model, tool and MCP call in that turn on a trace that started nowhere and named no session. `SetupOTel`'s "mast opens no custom spans, ADK emits the tree" was written when that was a v0.1 simplification and had quietly become the defect. One `mast.turn` span now opens at the shared chokepoint, which fixes the parenting and buys the only place to hang turn-grain facts: `mast.session.id` (a span attribute precisely because it is banned as a metric label), `mast.turn.outcome`, `mast.cost.usd` for this turn's delta, and `mast.turn.queued_ms`, since one session runs one turn at a time and time spent behind another turn otherwise reads as a slow model. **Two restraints carry the decision.** The existing `kind:detail` turn label is **split, not stored whole** — every producer writes one (`inject:<reason>`, `scheduled:<RFC3339>`, `autoresume:<session>`) and stored whole the groupable half is unusable, because no backend groups by a string with a timestamp in it. And the span opens **before the turn lock**, so a turn refused at the chokepoint leaves a record — under a `refused` outcome that is **span-only**: `mast_turns_total` has only ever counted turns that started, and quietly widening it would move every dashboard's denominator. Span outcome and metric outcome now come from one call, so they cannot drift. **No equivalent of upstream's inject→turn span *links* (core-agent `a58fdcc`) is needed or wanted**: links are for asynchronous fan-in, where an inbox batches injects and one turn answers several so no single parent edge is truthful; mast dispatches an inject synchronously on the request goroutine, so the inject's server span is already the real parent and a link would be a weaker statement of a fact mast can state exactly | [`../cmd/mast/turnspan.go`](../cmd/mast/turnspan.go) + [`./observability-design.md`](./observability-design.md) attribute vocabulary |
| **An action that documents a mutation needs a door for it, and a mutation nothing enforces is refused** (2026-08-20, closing [#216](https://github.com/go-steer/mast/issues/216)). `auth.ActionSessionAdmin` has described itself as covering "ACL / metadata mutations on the session" since the matrix was written, and the only route behind it was `DELETE`: an ACL could be granted at creation and never afterwards amended, so adding a viewer to a running session meant destroying the session. `GET`/`PUT /sessions/{id}/acl` close it — `GET` at the **read** bar, since everyone the ACL admits can already read the transcript and a viewer needs to know whom to ask; `PUT` at the admin bar, as a whole-document replace, because omission-means-leave-alone would leave no way to remove the last viewer. Three things fell out of writing it. **`PUT` is refused with 501 when the daemon does not enforce ACLs** — the same shape as [#211](https://github.com/go-steer/mast/issues/211)'s inert allowlist, one layer up: an amendment nothing consults would report success for a restriction that does not exist, so the response carries `enforced` and `persisted` rather than a bare 204. **Ownership transfer is daemon-admin only and fails loudly**, since a dropped `owner` field in an accepted request reads as a completed transfer, and an owner cannot be cleared at all (a session no one owns is a lockout wearing an edit's clothes). **`Entry.ACL` had to stop being a field**: it was written once at registration and read without a lock by every authorizing request, which a runtime amendment turns from a stale read into a torn one — now an accessor returning cloned slices, written only through the registry, which persists before it mutates memory | [`../DESIGN.md`](../DESIGN.md) + core-agent's `docs/multi-session-design.md` |
| **A grant is published by its effect, because the declaration's meaning is a distinction JSON erases** (2026-08-21, closing [#218](https://github.com/go-steer/mast/issues/218)). `GET /sessions/{id}/subagents` is the surface built to answer *what can this thing do*, and it carried `capability` — allowed to change anything, yes or no — with no tool grant beside it, so the other half of the question had no answer short of reading the bundle. Entries now carry a `tools` object, and **the shape of it is the decision**. `ToolAllowlist`'s semantics are presence-per-axis: no `mcp:` key inherits every toolset the workload has, `mcp: []` denies them all, and both are the same empty array on the wire — opposite grants, one character apart, and it is the inherit-everything one that reaches the cluster. So the projection reports `mcp_grant: all|none|listed` rather than transcribing the list, and `whole_server` draws the same distinction one level down, where `filterToolsets` passes a listed server whole if its entry declares no `tools:`. A catalog that had just copied the frontmatter would have rendered the deny-all specialist and the reach-everything one identically. **The built-in axis ships as `builtin_declared`, not `builtin`**, and the name is doing real work: nothing populates `specialists.BuildOptions.Tools`, so a specialist is built holding no built-in tools and the list narrows nothing — what reads it is the write gate and the capability-split check, as a declaration *about* the spec. Publishing it under the table's name would tell an operator the specialist can call tools it does not hold; the gap itself is [#219](https://github.com/go-steer/mast/issues/219), [#211](https://github.com/go-steer/mast/issues/211)'s shape one notch weaker (the axis is read, it just does not do what the table says). The third axis, `skills`, gets no field at all rather than an always-empty one, since #211's loader refuses a non-empty list and it can never reach a running roster. No protocol bump: no frame changed | [`../pkg/attach/state.go`](../pkg/attach/state.go) `SubagentToolGrant` + [`../cmd/mast/subagentcatalog.go`](../cmd/mast/subagentcatalog.go) |
| **A package with no caller cannot be fixed, and the surfaces that imply it has one are the defect** (2026-08-21, verdicting core-agent `3de4134`, the last carry-forward item of the 2026-08-20 triage, and opening [#221](https://github.com/go-steer/mast/issues/221)). Upstream's fix carries the digest subagent's cache buckets into the paying session's usage, and **the defect it fixes is real in mast's copy of the struct**: `digest.Savings` has `SubagentModel`/`SubagentInputTokens`/`SubagentOutputTokens` and no cache buckets, so a subagent on a cache-warm model bills the session at the uncached rate. It is also unobservable here, because **`pkg/digest` has zero importers** — absent from `go list -deps ./cmd/mast`, because the MCP wrapper that drives digesting upstream was never ported and the descope was never recorded as one. Porting the buckets would be three more fields nobody fills in a struct nobody writes: [#211](https://github.com/go-steer/mast/issues/211)'s shape a third time, after [#219](https://github.com/go-steer/mast/issues/219). **So the deliverable is the accuracy pass, not the port.** Three surfaces described a live feature — `attach.UsageInfo.DigestMethods` ("present when at least one `digest.Process` call has fired", i.e. never), the attach protocol's v1.2.0 `latency_ms` and v1.3.0 `savings` tool-result sidecars (specified as produced by `pkg/mcp/digest_wrap.go` and `pkg/mcp/namespace.go`, neither of which exists here), and `Savings.Subagent*` — and each is annotated where it lives rather than in a design doc nobody reads at the call site. **The claim is held by a test rather than a comment**, on the standing lesson that an unexercised declaration is not enforcement: `TestNothingInMastImportsDigest` parses every `.go` file in the repo and fails the day one imports the package, naming the three annotations to correct and asserting a floor on the file count so an empty walk cannot pass it. The wire fields stay on the response shape: the surface is wire-compatible with core-agent's daemon, which does populate them, and mast-web decodes both. Two fixes rode along, both safe *because* nothing calls the package — the span attributes went `core_agent.digest.*` → `mast.digest.*`, matching [`./observability-design.md`](./observability-design.md)'s vocabulary, and six references to core-agent's `digest-design.md` / `agentic-mcp-design.md` plus three cross-repo issue numbers that collide with real mast issues (`#128`, `#130`, `#223`) are qualified | [`../pkg/digest/unwired_test.go`](../pkg/digest/unwired_test.go) + [`../DESIGN.md`](../DESIGN.md) package table |
| **The ACL is the registry's own state, so nothing outside it gets to hand one back** (2026-08-21, closing [#223](https://github.com/go-steer/mast/issues/223)). A session evicted for idleness or lost to a restart is rebuilt by a `SessionResumer` on the next request, and `resumeAndRegister` registered the rebuilt entry under the ACL that resumer returned — having, two lines earlier, read the persisted row and authorized the caller against it. The row was read for the gate and then dropped. **A resumer returns the zero ACL**, because it is a factory for the Registrant and the ACL is durable state it has no reason to re-fetch; mast's own `storeResumer` does, and so will an embedder's. Zero means no owner, no owner means admins only, and the deny is 404-masked — so the owner's own request built the entry that then told them their session did not exist, and it stayed that way until the next eviction. [#216](https://github.com/go-steer/mast/issues/216)'s `PUT /acl` made it sharper: an amendment writes to disk *and* to memory, and memory is precisely what an eviction drops, so a viewer added at 4pm was gone after the overnight restart. **The row wins whenever a store is wired** — the resumer's value survives only where there is nothing else (no store, no row), which is the storeless and legacy paths. Deliberately *not* widened: a store read that fails stays fatal only when a resume gate is present, since a blip is no reason to refuse a resume nobody was going to authorize. The same discard also swallowed every resumed session's `LastTouchedAt` update, `registerResumed`'s `Touch` being guarded on a non-empty owner. Unreachable through the shipped daemon (which wires neither an ACL store nor `MultiSessionEnabled`) and reachable by any embedder that turns both on; missed for as long as it existed because the test double re-read `row.ACL()` by hand, **better behaved than any resumer anyone writes** | [`../pkg/attach/registry.go`](../pkg/attach/registry.go) + [`../pkg/attach/registry_resume_acl_test.go`](../pkg/attach/registry_resume_acl_test.go) |
| **`tools.builtin` is a claim the spec makes about itself, and the one consumer that treated it as a grant was the bug** (2026-08-21, closing [#219](https://github.com/go-steer/mast/issues/219)). [`./specialists-design.md`](./specialists-design.md)'s normative table reads the builtin axis the way it reads the MCP one — absent inherits all, empty denies all, non-empty is a whitelist — and **nothing populates `specialists.BuildOptions.Tools`**, so a specialist is built holding no built-in tools at all and there is nothing for the axis to narrow. mast's built-ins are the planner's control-plane vocabulary, wired onto the *root* under planner dispatch and never offered to a specialist. Given the choice between making the table true and making it honest, this is the honest half: the axis is documented as a declaration, with the same promise-vs-reality table [#211](https://github.com/go-steer/mast/issues/211) got, and absent and empty are stated to mean the same thing here — which is the opposite of the MCP axis, where the distinction is the whole design. **The field is kept, not refused as #211's `skills` was**, because unlike skills it has three real consumers, and all three read it in the *refusing* direction: the capability split and the fan-out branch check both reject a read_only spec that names a mutating tool here, and the startup log reports it as declared write surface. Holding a specialist to a claim costs nothing when the claim turns out to grant nothing. **The consumer that ran the other way is the fix.** The write gate's producer contract folded builtin names into `changeSurface`, the set a `proposed_change` may name — so an executor declaring `builtin: [patch_k8s_resource]` had proposals for it *accepted at report time* and then nothing to run them with, which is precisely the silent-approval failure the contract's own doc comment exists to describe. The executable surface is now the MCP allowlist alone; an executor whose whole declared surface is builtin gets an empty one, refuses every proposal, and **says so at startup** rather than letting an operator discover it mid-incident. Untested behavior, which is why it survived: no test in the repo exercised the builtin branch. The remaining half — populate `BuildOptions.Tools` and let the axis narrow for real — is a feature needing a built-in set to exist first, and stays open | [`../internal/compose/writegate.go`](../internal/compose/writegate.go) `changeSurface` + [`./specialists-design.md`](./specialists-design.md#the-builtin-axis-is-a-declaration-not-a-grant-2026-08-21) |
| **A replay is keyed on the branch, because the branch is ADK's own name for "these run at the same time"** (2026-08-21, closing [#229](https://github.com/go-steer/mast/issues/229), verdicting core-agent `f90bc65`). `mock.NewScripted` returned one cursor; `internal/compose.BuildModel` collapses every per-specialist model override back to the root instance when the root is an offline fake — correct for the stateless fakes, and for `scripted` it means a whole roster shares one *position*; `parallelagent` then runs the fan-out branches concurrently. Three analysts walked one script between them. **The mutex made it worse, not better**: `-race` stayed silent while the assignment of recorded turns to branches depended on goroutine scheduling, and the symptom was a `script exhausted` from whichever branch lost — or, quieter, a branch that succeeded having replayed another branch's turn. Upstream gave every `Model` *call* its own cursor; mast keys on the **branch** instead, and the difference is multi-turn replay: a second user turn must go on consuming the same recording rather than restart it at turn 0. The branch is the right key because it is ADK's own concurrency-isolation identity — `parallelagent` composes `<parent>.<subagent>`, the workflow worker and dynamic scheduler each mint one, and the sequential scheduler propagates its parent's unchanged — so every sequential shape (coordinator, planner, single-agent, resume) is provably byte-identical to before, and only the shapes that genuinely overlap get separate cursors. **The seam is the model's own context**, which ADK hands in as an `agent.ReadonlyContext`: the fix is one package, with nothing plumbed through `internal/compose`, `pkg/specialists` or `pkg/graph`. Not the `ModelResolver`, which `modelFor` bypasses for any spec declaring no `model:`. Each replay decodes the transcript for itself, since recorded responses are pointers handed straight into the agent loop. Two documented non-goals: one transcript cannot describe N *different* branches (that needs a recorded turn to name its agent, a `RecordedTurn` wire-format change mast shares with core-agent's `pkg/recording` and which therefore goes upstream first), and two concurrent *invocations* in one process still share the unbranched replay — a recording describes one session. Held by the shape that produced the defect: a three-analyst fan-out against a **one-turn** recording, which cannot pass at all under a shared cursor | [`../pkg/providers/mock/scripted.go`](../pkg/providers/mock/scripted.go) + [`../pkg/graph/fanout_scripted_test.go`](../pkg/graph/fanout_scripted_test.go) |
| **A dispatch is watched by the session's watchdog, and a trip there stops the session as well as the dispatch** (2026-08-21, closing the watchdog half of [#226](https://github.com/go-steer/mast/issues/226)). The metering half gave `invoke_specialist`'s private runner a way back to the host; the watchdog could not use it. Its enforcer taps a stream (`watchdog.Tap`) and a dispatch has no stream to wrap, so **the fix was to give the stream's per-event body a name**: `ObserveInto` and `Drain` are exported and `Tap` is re-expressed over both, which is what stops the two paths from drifting — a second hand-rolled observation loop is how the dedup rule and the end-of-run drain get out of step. `SubRunObserver` was reshaped from a flat callback into a **sink opened per dispatch**, free to change because it shipped hours earlier and is still unreleased. A meter is cumulative and was happy flat; the watchdog is not, on two independent counts — it dedups an aggregator's re-emissions *within* a run and counts repetition *across* runs, so it needs the boundary, and a flat callback cannot correlate two parallel dispatches interleaving into one method at all. **Observation goes to the session's watchdog, not one minted per dispatch**, because the signals count repetition and a per-dispatch watchdog resets that count every time: three identical calls in each of ten dispatches would never reach a threshold of five. The cost is stated rather than hidden — the planner's own calls and its specialists' now share one signal set, so an `invoke_specialist` between two dispatches breaks a consecutive run, which is the interleaved shape `DominantToolCallSignal` ([#227](https://github.com/go-steer/mast/issues/227)) exists for and the same trade-off the outer stream already makes between a coordinator and its sub-agents; the loop this catches, a specialist spinning inside *one* dispatch, has no interleaver. **And a trip fires both levers, deliberately unlike the budget half**: a ceiling is cumulative, so stopping the sub-run is the whole remedy, while a trip is a latch meaning an operator must reset the session, and every other door to it refuses the whole session — a halt that stopped only the dispatch would let the planner re-dispatch the same specialist immediately, which is the treadmill enforce exists to break. Under warn and feedback nothing is cancelled, since `Enforcer.Observe` reports a halt only under enforce; the model-facing paragraph goes to the **planner**, the specialist's sub-session being already gone and the planner being the one that would dispatch again. Proved by mutation rather than by coverage: five deliberate breaks, five failing tests. **What this does not close** is the same seam read one level wider — runner *plugins* are runner-scoped and the dispatch tool builds its runner with none, so a mutating call inside a dispatch reaches neither the outbox nor the write gate, measured and filed as [#235](https://github.com/go-steer/mast/issues/235) | [`../pkg/watchdog/bridge.go`](../pkg/watchdog/bridge.go) + [`../cmd/mast/subrun.go`](../cmd/mast/subrun.go) |
| **A gate that cannot reach a call refuses the configuration instead of the call** (2026-08-21, containing [#235](https://github.com/go-steer/mast/issues/235)). Runner plugins are runner-scoped, and `invoke_specialist` builds its runner itself — so under planner dispatch a mutating call reaches neither the effect outbox nor the **write gate**, and a bundle asking for `require_approval` got no park and one asking for `dry_run` got a real write. Measured with a probe rather than argued from the code: an outer `BeforeToolCallback` sees `invoke_specialist` and `finish_task` and never the `scale_deployment` executed between them. **The fix is not mechanical** — passing the host's plugins to the sub-runner installs a gate that parks, and a park suspends a turn to be resumed from a durable session while a sub-session is in-memory, so #235 is a question about where the boundary sits (host session service for the sub-run / gate at the dispatch boundary / refuse the combination) and not a wiring change. **So the containment shipped and the fix did not.** `CheckPlannerWriteSurface` refuses a planner roster holding a `change_executor` whenever `hitl.on_mutation` asks for the write to be gated, on both doors — `BuildRoot` for the library path and the pre-MCP `CheckRoster` for the binary's, because a write-path refusal is the last one an operator should have to read past an OAuth 403 to find. **Refuse rather than warn**, the same argument `CheckNameCollisions` makes: a warning is a line in a log nobody tails on a deployment nobody watches, and the operator would go on believing they had approval. **`apply` is exempt**, because the refusal is scoped to the promise actually broken — under `apply` no gate was going to stop the call, and what remains missing is the outbox record, named at the call site and in the reference page rather than refused over. The escape is cheap and in the message: the same roster runs under `coordinator` or `graph`. `examples/workloads/gke-triage` ships `require_approval`, a `change-executor`, and a commented-out `planner:` block — two lines from the combination, so the comment now says why uncommenting it stops | [`../internal/compose/capability.go`](../internal/compose/capability.go) `CheckPlannerWriteSurface` + [`../internal/compose/plannerwrite_test.go`](../internal/compose/plannerwrite_test.go) |
| **The optional half of an upstream feature is what made it unportable, so mast ports the half that is free** (2026-08-21, closing [#221](https://github.com/go-steer/mast/issues/221) — the row *"A package with no caller cannot be fixed"* opened it, and this one answers it). #221 framed wiring `pkg/digest` as costing a small-tier-model config surface, a second billing path inside a tool call, and a budget story for spend the session's meter never sees — the price of upstream's `LLMFallback`. Reading upstream settled it: **`DigestOptions.LLMFallback` is optional**, nil means structural-only with a bounded passthrough, and core-agent itself ships structural digest **on by default** with the fallback off. So mast wires the wrap with no `LLMFallback` field at all. That buys the package a caller, makes `attach.UsageInfo.DigestMethods` and the v1.2.0 `latency_ms` / v1.3.0 `savings` sidecars true instead of aspirational, and turns core-agent `3de4134`'s verdict from *indefinitely blocked* into *lands with an LLM fallback or not at all* — `Savings.Subagent*` are now zero **by construction** rather than by absence. **One deliberate divergence from upstream:** below the 8000-byte threshold mast returns the tool's own response map untouched rather than wrapping it as a passthrough digest, because a wrapped passthrough re-serializes the map into a JSON *string* and costs more tokens than the map it replaced — doing that to the small responses, which are most tool calls, to compress the large ones inverts the point. The cost is stated where it lands rather than discovered: `digest.Telemetry()` counts **routed** calls, not tool calls. **"Untouched" had to become literal**: the first cut stamped a `latency_ms` sidecar on every response including the undigested ones, and the v0.4 UAT went intermittently red on a leg nobody had connected to digesting — `pkg/approval` voids a change-set grant when the precondition read stops returning what it returned at approval time, and it compares a hash of the *whole* result, so a wall clock in an otherwise unchanged response revoked the operator's approval whenever two reads landed in different milliseconds (the declared-field diff came up empty, which is why the failure said only "no longer returns what it returned"). The general rule the incident settles: **a wrapper that a value-equality comparison can see through must add nothing on the path it does not transform.** Latency lives on the `mcp.tool_call` span, which costs no caller anything, and the digest envelope keeps its own copy because that response is mast's construction end to end. The second half is the mirror image — the same read must not be *digested* either, since a digest envelope carries a fresh `call_id` per call and drops the compared fields, so `digestingTool` exposes `Unwrap()` and `cmd/mast`'s precondition reader peels the wrap before running the tool. Both halves are pinned by tests that fail on the pre-fix code. **`retrieve_raw` reaches specialists as a `tool.Tool`, not a toolset**, and that is the load-bearing delivery decision — `specialists.filterToolsets` matches a toolset to a `tools.mcp: - server:` entry by `Name()` and drops it when there is no match, so a toolset-delivered escape hatch would have been silently filtered out of exactly the rosters that enumerate their tools, the posture mast recommends. It widens no grant: it returns bytes the specialist already received. The store is scratch state under `os.TempDir()` (house rule #5) and a store that cannot be created leaves `retrieve_raw` unregistered with a warning — a registered tool that refuses every call teaches the model only that the escape hatch does not work. `TestNothingInMastImportsDigest`, the tripwire that instructed its own deletion once something imported the package, is deleted; the three annotations it named are corrected in place | [`../pkg/mcp/digest_wrap.go`](../pkg/mcp/digest_wrap.go) + [`../pkg/mcp/retrieve_raw.go`](../pkg/mcp/retrieve_raw.go) + [`../DESIGN.md`](../DESIGN.md) package table |
| **A guard that a supplier's bad minute can trip is measuring the supplier, so fix the run rather than the guard** (2026-08-21, closing [#239](https://github.com/go-steer/mast/issues/239)). `evals-nightly-gemini` went red on its first bad night after four greens: three of 31 scenarios came back `Error 429 ... RESOURCE_EXHAUSTED` from Vertex, so the board scored 28 rows and the judge tier raised its "the board is missing a row rather than scoring one" problem, which is exit 1 by design. The tempting fix — let the gate tolerate a partial board — is the wrong one and was rejected: 28 rows compared against a 31-row baseline understates or overstates whatever is missing, so tolerating it would cost the reporting tier the one thing it *can* honestly fail on. **The defect was upstream of the gate: nothing tried to get the row back**, on a status whose own error message links to Google's back-off-and-retry guidance. `judge.Retrying` now wraps both models the tier builds — corpus, grader and J-cost-tier — and waits out a `429`/`503` on a bounded 3s/9s/27s schedule, bounded because a nightly with a 90-minute budget and 31 sequential metered rows must fail and say the board is short rather than wait out a real outage. Three shape decisions carry beyond the evals: **(1)** the retry window closes the instant the first response is yielded, because a stream that already spoke cannot be replayed without handing the caller its content twice; **(2)** retryability is decided by `errors.As` on each SDK's own error type and never by matching text — the corpus is 31 Kubernetes incidents, several about exhausted resources, and the grader is handed those responses verbatim, so a substring classifier can be fooled by the payload it is grading; **(3)** the retries are counted onto the board, printed when non-zero and diffed night over night, because **a retry nobody can see is how a measurement quietly stops measuring** — a provider under worsening pressure would otherwise keep producing complete, green, increasingly slow boards with nothing to point at. The incident also exposed an asymmetry invisible from mast's side: `anthropic-sdk-go` retries twice of its own accord and `google.golang.org/genai` does not, so the two nightlies had been measuring different amounts of resilience and the Anthropic board on the same commit was green. Retrying at `model.LLM`, the one interface both providers arrive through, is what makes the two boards comparable | [`../internal/evals/judge/retry.go`](../internal/evals/judge/retry.go) + [`../.github/workflows/evals-nightly.yml`](../.github/workflows/evals-nightly.yml) |

## Open questions still on the table

Each doc has its own open-questions section. The strategic ones (cross-doc impact):

- **core-agent's own positioning rewrite** ([`./fork-design.md`](./fork-design.md)). Under (E), core-agent needs its own positioning doc — sibling to [`./positioning.md`](./positioning.md) — written for its actual audience (experimentation/integration).
- ~~**Which side of the fork watchdog→model routing lives on**~~ *Resolved 2026-08-17: port all seven watchdog commits and reclassify the routing as **shared infrastructure**. See the resolved-decisions row below and [`./fork-design.md`](./fork-design.md) § "Sync discipline under (E)".*
- ~~**Small-tier-parent classifier aging**~~ *Resolved 2026-07-01: LLM-as-router with a `SingleTurn` LlmAgent replaces the substring matcher and ages gracefully. See [`./positioning.md`](./positioning.md) open Q #4 update and [`./workflow-scaffolding-design.md`](./workflow-scaffolding-design.md)'s LLM-as-router section.*
- **Where provider profiles live, and whether they outrank the Go tier tables** ([`./model-support-design.md`](./model-support-design.md) OQ-1/OQ-2, settled in [`./config-layout-design.md`](./config-layout-design.md)). Replacing prefix-inferred provider dispatch with declared profiles needs a home in `.agents/` and a precedence rule against the hard-coded `taskclass.ModelForTier` table; config-layout owns discovery order, so this design should not invent a second convention.
- **Which side of the fork multi-dialect provider support lives on** ([`./model-support-design.md`](./model-support-design.md) §9, [`./fork-design.md`](./fork-design.md) § "Sync discipline under (E)"). core-agent has most of the same seams — mast's are ports of them — so shared-infrastructure vs lean-fork-specific is the same call the watchdog→model-routing question answered on 2026-08-17. Wants an answer before the first new adapter, not after.
- **Canonical positioning name** ([`./positioning.md`](./positioning.md)). "Agent infrastructure" / "platform agent runtime" / "agent substrate" — pick one and use consistently in the README sweep.
- ~~**Task-class name for `SingleTurn` mode**~~ *Resolved 2026-07-01: SingleTurn is internal, not user-facing. Public task classes remain chat/debug/implement/research/review + new `orchestrate` (planner-enabled workloads). See [`./orchestration-design.md`](./orchestration-design.md) task-class-resolution section.*
- ~~**Plan-first gate interaction with workflow graphs and planner**~~ *Resolved 2026-08-12 ([`./workflow-scaffolding-design.md`](./workflow-scaffolding-design.md) open Q #5, settled in [`./orchestration-design.md`](./orchestration-design.md)'s mutation-approval section): plan-first does **not** reach into graphs. The hazard it would be reaching in to prevent — unreviewed mutation — is held at a different seam by `hitl_policy.on_mutation`, which gates at tool execution regardless of the shape wrapped around it. Extending plan-first inward would be a second, weaker control over ground already covered.*

The doc-local open questions (specialist-API details for `agenttool`, sync discipline edge cases, per-shape testing conventions, etc.) live in each doc and don't affect the others.
