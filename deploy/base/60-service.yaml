# Copyright 2026 Google LLC
# Licensed under the Apache License, Version 2.0. See LICENSE for details.
#
# Originally derived from
# go-steer/core-agent@c5efbb9e:examples/gke-troubleshoot-agent/deploy/base/60-service.yaml.
apiVersion: v1
kind: Service
metadata:
  name: mast
  namespace: mast-triage
  labels:
    app.kubernetes.io/name: mast
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mast
    app.kubernetes.io/component: daemon
  ports:
    - name: inject
      port: 7777
      targetPort: inject
      protocol: TCP
