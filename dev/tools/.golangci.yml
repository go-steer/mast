# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# golangci-lint v2 config for mast. Mirrors core-agent's
# dev/tools/.golangci.yml (same conservative linter set, same
# settings) minus that repo's lift-specific path exclusions, so the
# two repos enforce the same bar and ported code lints identically on
# both sides.
#
# Run with:   dev/tools/lint-go
# Auto-fix:   dev/tools/lint-go --fix

version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    # Bug-finders we never want to silence.
    - errcheck       # unchecked errors
    - govet          # core vet rules
    - ineffassign    # ineffectual assignments
    - staticcheck    # massive bug+style ruleset (now includes stylecheck + gosimple)
    - unused         # dead code

    # Quality-of-life.
    - misspell       # common typos in identifiers/comments
    - unconvert      # unnecessary type conversions
    - unparam        # unused function parameters
    - gocritic       # opinionated extra rules; many useful, low false-positive

    # Compliance.
    - goheader       # enforces the Apache 2.0 boilerplate on every .go file

    # Security.
    - gosec          # OWASP-style checks (nosec via // #nosec)

  exclusions:
    # Generated code shouldn't fail the lint.
    generated: lax
    presets:
      - common-false-positives
      - legacy
    rules:
      # Tests can be looser about errcheck (eg. defer f.Close()) and
      # have permission to use weak random / hardcoded creds.
      - path: _test\.go
        linters:
          - errcheck
          - gosec
          - unparam
      # Lifted from go-steer/cogo via core-agent's pkg/permissions
      # (P1.3a port). The cogo attribution is preserved as explicit
      # provenance — same exclusion core-agent carries, same Apache-2
      # license both sides. Remove if the files are ever re-headered
      # upstream.
      - path: permissions/(verb|builtin_allow)(_test)?\.go
        linters:
          - goheader

  settings:
    errcheck:
      # Noise patterns where checking the error has no useful action:
      # writing to stderr for a diagnostic, closing a file in defer,
      # or best-effort cleanup of a temp file. The `_ =` boilerplate
      # is worse than the missed check here.
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Fprint
        - (*os.File).Close
        - os.Remove
    gocritic:
      disabled-checks:
        # "ifElseChain" pushes us toward switch statements that aren't
        # always clearer; let the author choose.
        - ifElseChain
        # commentFormatting is noisy for // TODO style markers.
        - commentFormatting
    gosec:
      excludes:
        # G104 (errcheck) is already covered by errcheck linter.
        - G104
        # G306 (WriteFile permissions): we deliberately use 0o644 for
        # scaffolded files so they behave like normal repo files.
        - G306
    goheader:
      # Enforced on every .go file. Ported files carry an
      # "Originally derived from go-steer/core-agent@<SHA>" line (the
      # P1.3 attribution convention) as its own comment group BELOW
      # this header — goheader only inspects the first comment group,
      # so attribution and full header enforcement coexist. goheader
      # cannot express an optional template suffix, which is why the
      # trailer must not live inside the header block itself.
      template: |-
        Copyright {{ YEAR-RANGE }} Google LLC

        Licensed under the Apache License, Version 2.0 (the "License");
        you may not use this file except in compliance with the License.
        You may obtain a copy of the License at

            http://www.apache.org/licenses/LICENSE-2.0

        Unless required by applicable law or agreed to in writing, software
        distributed under the License is distributed on an "AS IS" BASIS,
        WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
        See the License for the specific language governing permissions and
        limitations under the License.

formatters:
  enable:
    - gofmt          # canonical formatting
    - goimports      # import grouping + missing imports
  settings:
    gofmt:
      simplify: true
    goimports:
      local-prefixes:
        - github.com/go-steer/mast

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
